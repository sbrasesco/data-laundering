# DataLand V2.0 — Contexto del proyecto

## 🧭 División de trabajo (leer primero)

- **Cowork (este entorno)**: DB (Supabase MCP: migraciones, RPCs, triggers, queries), código (frontend + worker: leer/editar), análisis y documentación (tablero Trello «Ágora — Kanban» https://trello.com/b/tYu0PDIQ + este CLAUDE.md; el kanban de Notion quedó congelado el 2026-09-19 por el límite del plan gratuito). **No hace** deploy/git/SSH.
- **Claude Code CLI**: servidor y despliegue — `git`, `ssh` al VPS, `npm run build`, `scp` frontend, `docker compose` worker. Cowork le pasa los comandos.
- **No mezclar**: Cowork prepara (DB directo; código en el repo); Claude Code buildea/commitea/deploya.
- **Sync OneDrive**: hay latencia entre lo que Cowork edita y lo que Claude Code/git ven. Si un build no toma un cambio, esperar/reintentar y verificar con `grep`.
- **Riesgo de truncado al editar (2026-06-23)**: en archivos grandes de OneDrive, una edición puede dejar el archivo **truncado en disco** (pierde el final) mientras la relectura muestra una versión vieja/completa. Verificar SIEMPRE con build/parse (`npx esbuild <archivo> --loader:.tsx=tsx --bundle=false --format=esm --outfile=/dev/null` o `npx tsc --noEmit`) + balance `()`/`{}` + `git diff --stat` antes de commitear. Para edits grandes en esta carpeta, preferir escribir vía python/bash. (Caso real: MonitoringPage.tsx, CLAUDE.md y DocumentsTable.tsx se truncaron el 2026-06-23.)

> **Regla de oro**: las zonas cerradas no se tocan salvo tarea explícita. Ante la duda, señalar impacto y **preguntar ANTES de ejecutar**. Producción con clientes activos: estabilidad > velocidad.

---

## 🔒 ZONA CERRADA — no tocar sin tarea explícita

### Auth / Frontend loading
- `AuthContext.tsx` — callback `onAuthStateChange` **SÍNCRONO** (sin async/await). Supabase v2 retiene el session lock durante el callback: si es async y espera `fetchProfile()` (~19.5s), `getSession()` se bloquea igual → spinner 20s. `setLoading(false)` inmediato al conocer sesión; `fetchProfile` en `Promise.resolve().then(...)` (fuera del lock), fire-and-forget, NO llama `setProfile(null)` en error, reintenta 3× (1.5s/3s, timeout 5s). Solo `signOut`/sesión nula limpian profile.
- **Patrón hooks con guard** (`useTenantCredits`, `useClientJobs`): `if (authLoading) return` → `if (!organizationId) return` (mantener `loading=true`, el profile llega en background); el `loading` retornado incluye `|| authLoading`. Sin esto los componentes parpadean con ceros. Ambos tienen Realtime + polling fallback (15s/8s).
- `supabase.ts` — usa `window.__nativeFetch` (SDKs como Amplitude parchean `window.fetch` y lo rompen). No reemplazar por `fetch` directo.
- `index.html` — spinner inline + guard `__nativeFetch` en `<head>` antes de cualquier recurso externo; mensaje "Tardando demasiado" a los 8s. `<ErrorBoundary>` en `main.tsx` + handler `unhandledrejection`. No mover/eliminar.
- `AppShell.tsx` sidebar balance — 3 estados: skeleton (`creditsLoading`) → rojo "Sin saldo" (`balance<=0`, es un `<button>` que abre `InsufficientCreditsModal`) → verde con monto. No convertir en `div`.

### Billing / Pagos
- ⚠️ **2026-09-18: el flujo de compra NUNCA acreditó una compra en producción** (8 preferencias, todas de Aignition, todas `pending` — enlaces que abrió el director para comprobar que el checkout abre, **no pagos fallidos**; cero `purchase` en `credit_transactions`). Lo de abajo describe el diseño, no algo probado en prod. Se rehace según `ESPEC-COMPRA-DE-SALDO.md` v3 (tablero, BILLING-COMPRA-0 a 6).
- **Diagnóstico del webhook (BILLING-COMPRA-0, cerrado 2026-09-19):** nunca corrió en producción (MP no tiene ningún pago de esas preferencias; cero `mp.webhook.*` en los registros). Tal como está hoy: no usa `credits_accrued`; marca `approved` **antes** de acreditar (si `add_credits` falla, el reintento sale por «ya procesado» y el saldo no se entrega); responde 200 aun cuando falla (MP no reintenta); verifica reconsultando el pago a la API de MP, sin firma. Lo reemplaza `credit_payment` (ESPEC §5.2). Ya existe índice único `payments_gateway_payment_id_idx` (donde no es nulo).
- **Modelo de precios nuevo (BILLING-COMPRA-1, aplicado y validado 2026-09-19; migraciones `billing_compra_1_modelo_precios_tablas` y `_funciones`):** tablas globales `pricing_settings`, `pricing_packages`, `pricing_currencies`, `fx_rates` (RLS sin políticas, **ningún permiso** para `anon`/`authenticated`: se accede por funciones). 10 columnas nuevas, nulas, en `payments` (`base_usd`, `bonus_usd`, `credited_usd`, `package_code`, `fx_rate`, `fx_source`, `fx_fetched_at`, `expires_at`, `origin_product`, `credited_at`) y tipo `bonus` en el libro. `quote_purchase(moneda, paquete | monto)` sólo calcula (EXECUTE: `authenticated`, `service_role`); `admin_get_pricing`, `admin_set_pricing_settings`, `admin_upsert_package`, `admin_set_currency` con candado de superadmin. Arranque: base US$ 50; `basico` ×1 0 %, `profesional` ×2 5 %, `business` ×4 10 %; libre US$ 5–2.000 (**hoy: mínimo US$ 10 y bonos 0 / 10 / 20 %** → Básico US$ 50 → 50, Profesional US$ 100 → 110, Business US$ 200 → 240; decisión del director 2026-09-19, cargada con su usuario por `admin_upsert_package`; el bono es siempre un porcentaje del monto que se paga; la landing se corrigió para decir lo mismo); ARS encendido (Mercado Pago, BNA venta); USD y EUR apagados (no se pueden encender hasta integrar su pasarela). `fx_rates` está **vacía a propósito**: la llena el servidor desde la fase 3; hasta entonces `quote_purchase` en pesos responde «todavía no hay cotización». **Nada de lo que corre hoy lee estas tablas.**
- **`credit_payment(payment_id, gateway_payment_id)` (BILLING-COMPRA-2, aplicado y validado 2026-09-19; migración `billing_compra_2_credit_payment`):** sólo `service_role`. Bloquea el pago; si ya está acreditado devuelve `already_credited` y no hace nada; **suma** `credited_usd` al saldo que ya tiene la organización; escribe `purchase` (lo pagado) y `bonus` (lo regalado, sólo si es mayor que cero) con su `balance_after`; marca el pago `approved` + `credits_accrued` + `credited_at`. Todo o nada. Rechaza pagos sin montos congelados (los del camino viejo) y un identificador de pasarela ya usado en otro pago (índice único). La llaman el aviso de Mercado Pago (fase 4) y la revisión automática (fase 5.1).
- **Creación de pago nueva (BILLING-COMPRA-3, desplegada 2026-09-19 16:16 Madrid y validada; respaldo `gateway.mjs.bak-20260919-161631`):** `POST /api/purchase/create` en `worker/purchase.mjs`. Se autentica con la **sesión del usuario** (token de Supabase), no con la llave del gateway (la llave → 401). Orden: dólar del **Banco Central** (divisa venta, `api.bcra.gob.ar/estadisticascambiarias/v1.0/Cotizaciones/USD`, decisión del director) → `fx_rates` → `quote_purchase` → pago guardado con todo congelado → preferencia de Mercado Pago **en pesos**, vence a los 60 min, `binary_mode` → anota la preferencia. Si el Banco Central no responde: la última cotización guardada y `fx_source = …:respaldo`. Sin `GATEWAY_URL` no crea nada. URL de vuelta sólo de `FRONTEND_URL` + `PURCHASE_RETURN_ORIGINS`. Validado: Profesional → $151.450 por US$ 105 (dólar 1.514,50; con el bono de entonces, 5 %), visto por el director en Mercado Pago. **Ninguna pantalla la usa todavía** (fase 5). Un pago con `base_usd` **nunca** se acredita por el camino viejo (acreditaría los pesos como dólares): desde la fase 4 va por `credit_payment`.
- **Aviso de pago nuevo (BILLING-COMPRA-4, desplegado 2026-09-19 17:34 Madrid; respaldos `gateway.mjs.bak-20260919-173338` y `purchase.mjs.bak-20260919-173338`):** `POST /api/mp/webhook` entiende los tres formatos de aviso (cuerpo JSON, `?type=payment&data.id=…` y `?topic=payment&id=…` sin cuerpo; `parseMpNotification`, sólo números de pago numéricos). Reconsulta el pago a Mercado Pago. Para un pago del flujo nuevo compara monto, moneda y referencia con lo congelado: si coincide → `credit_payment`; si no → el pago pasa a `status='review'` con el motivo en `metadata.review` y **no se acredita**. Si Mercado Pago o la base fallan, o falla la acreditación → **500** para que Mercado Pago reintente (antes: 200 y el aviso se perdía). Pago inexistente (404) → 200. Si por preferencia no aparece, busca por `external_reference`. **El camino viejo (pagos del modal actual) acredita igual que antes.** Sin firma `x-signature` por ahora: la reconsulta impide acreditar un pago inventado. **Decisión del director: sin pago de prueba** (ni real ni sandbox); resguardos: 26 pruebas locales, avisos de prueba desde afuera después del despliegue y el **primer pago real vigilado**. Si el aviso no llega, lo carga la revisión automática (5.1, abajo); como último recurso, a mano con `credit_payment(payment_id, id_de_pago_de_MP)`, que no carga dos veces.
- **Revisión automática de pagos (BILLING-COMPRA-5.1, desplegada 2026-09-19 17:58 Madrid y validada; respaldos `gateway.mjs.bak-20260919-175757` y `purchase.mjs.bak-20260919-175757`):** `createPaymentReconciler` en `worker/purchase.mjs`, arranca con el gateway (`startGateway`). Cada 10 min (primera pasada al minuto de arrancar) toma los pagos del flujo nuevo (`base_usd` no nulo) en `pending`, sin `credits_accrued`, creados en las últimas 72 h (hasta 50 por pasada); le pregunta a Mercado Pago por `external_reference` y, si encuentra uno aprobado, lo acredita **por el mismo camino que el aviso** (`creditNewPayment`: compara monto, moneda y referencia → `credit_payment`; si no coincide → `review`). Lo que carga queda con `metadata.credited_by = 'reconcile'` + `reconciled_at` y deja `mp.reconcile.credited` (warn) en los registros; cada pasada deja `mp.reconcile.done {checked, credited, errors}`. No superpone pasadas. **No toca los pagos del camino viejo.** Primera pasada en producción: 2 revisados, 0 acreditados, 0 errores. Interruptor: `PURCHASE_RECONCILE_MINUTES=0` en el `.env` del worker + recrear el contenedor (`PURCHASE_RECONCILE_FIRST_DELAY_MS` mueve la primera pasada). Vuelta atrás completa: restaurar los dos respaldos `…175757` + `docker compose build` + `up -d --force-recreate`.
- **Lectura para la pantalla de compra (BILLING-COMPRA-5.2 paso 1, aplicada y validada 2026-09-19; migración `billing_compra_5_2_opciones_de_compra`):** `get_purchase_options(p_currency default 'ARS')` → jsonb. `SECURITY DEFINER`, `STABLE`, sólo lectura; `EXECUTE` para `authenticated` y `service_role` (anon → permiso denegado). Devuelve `currency`, `charge_decimals`, `min_free_usd`, `max_free_usd`, `fx_rate`, `fx_rate_date`, `fx_fetched_at`, `fx_source` y `packages` (activos, por `sort_order`: `code`, `label`, `base_usd`, `bonus_pct`, `bonus_usd`, `credited_usd`, `charged`). Cada paquete se cotiza con `quote_purchase` (una sola fuente de cálculo); **el monto libre la pantalla lo cotiza llamando a `quote_purchase` directamente**. Hace falta porque los usuarios no tienen permiso sobre las tablas de precios. Validado como usuario de Menara: los tres paquetes coinciden con `quote_purchase`; moneda no habilitada → error. Vuelta atrás: `DROP FUNCTION public.get_purchase_options(text);` (nada la usa todavía).
- **Estimación de documentos (verificado 2026-09-19, sin cambios):** barra lateral, tablero y Configuración usan `get_price_breakdown().total_per_doc` = precio base + extras activos del cliente. Hoy: Aignition US$ 0,85, Menara US$ 0,55. Ni el frontend ni esa función se tocaron en todo el trabajo de compra. La pantalla de compra nueva usa ese mismo número.
- **Pantalla de compra nueva (BILLING-COMPRA-5.2, desplegada 2026-09-19 en dos vueltas y validada por el director; respaldos del frontend `/var/www/dataland.bak-20260919-163934` = previo a 5.2 y `.bak-20260919-165044` = primera vuelta):** `InsufficientCreditsModal.tsx` reescrito con el mismo nombre y las mismas entradas (lo abren la barra lateral y la subida de ZIP). Lee paquetes, mínimo y máximo de `get_purchase_options`; el bono del monto libre lo calcula `quote_purchase` (una sola regla); documentos estimados con `get_price_breakdown().total_per_doc`; **muestra sólo dólares** + leyenda «Si pagás con Mercado Pago, el monto se convierte a pesos…» (decisión del director; los pesos se ven en Mercado Pago antes de pagar); compra por `POST /api/purchase/create` con la sesión del usuario y botones bloqueados hasta salir a Mercado Pago. Formato y lectura de montos en `src/lib/purchaseFormat.ts` (acepta «25», «25,50», «1.000»). Ventana `max-w-3xl` con los paquetes en fila (la primera vuelta quedaba cortada arriba y abajo). Validado: el director compró US$ 10 → Mercado Pago mostró $15.145 (pago `df52a593`, no pagado). Landing corregida a 50 / 110 / 240. **Landing y login también compran por la puerta nueva** (tercer despliegue 2026-09-19, respaldo `/var/www/dataland.bak-20260919-170350`): función compartida `src/lib/purchaseCheckout.ts` (la ventana «Recargar saldo» tiene su propia llamada, equivalente); la landing sin sesión manda a `/login?plan=<code>` y el login crea el cobro **una sola vez** aunque la sesión se actualice. Validado: landing → login → Mercado Pago «US$ 110,00 (incluye bono de US$ 10,00)» $151.450 (pago `f9c2bd2f`, uno solo, no pagado). **Ninguna pantalla usa ya `/api/mp/create-preference` ni `create-custom-preference`**: siguen en el servidor sin uso. Retirarlas es un paso aparte; ojo: el camino viejo del aviso sigue haciendo falta mientras haya preferencias viejas sin vencimiento que alguien pudiera pagar.
- **Editor de precios de la compra (BILLING-COMPRA-5.3, desplegado 2026-09-19 y validado por el director; respaldo del frontend `/var/www/dataland.bak-20260919-173810`):** Monitoreo → Precios → «Editor de precios», sección «Compra de saldo — paquetes y bonos» (`src/components/monitoring/PurchasePricingEditor.tsx`). Edita monto base, mínimo y máximo del monto libre y, por paquete, veces el monto base y bono %; muestra lo que paga y recibe el cliente y el dólar en uso (sólo lectura). Lee con `admin_get_pricing` y guarda sólo lo que cambió con `admin_set_pricing_settings` / `admin_upsert_package` (las tres exigen superadmin en la base). **Se guarda directo en la base** (`pricing_settings`, `pricing_packages`, con `updated_at` y `updated_by`; sin historial de valores anteriores) y la ventana de compra lo toma al instante; los pagos ya creados no cambian porque llevan sus montos congelados. Validado: el director puso el bono del Profesional en 12, vio «+ US$ 12 → US$ 112» en «Recargar saldo» y lo volvió a 10 (17:45 UTC). En la misma ventana, «Precio por documento» quedó en una sola fila (la del Básico, la única que cobran `charge_credit` y `get_price_breakdown`); las filas de Profesional (0,27) y Business (0,22) eran del camino viejo y no se usaban. La landing también sigue al editor desde 5.4 (abajo).
- **Landing con precios de la base (BILLING-COMPRA-5.4, desplegada 2026-09-19 en dos vueltas y validada por el director; respaldos `/var/www/dataland.bak-20260919-181336` = previo a 5.4 y `-181721` = primera vuelta; migración `billing_compra_5_4_precios_publicos`):** `get_public_pricing()` → paquetes (monto, bono, lo que recibe), precio base por documento (`billing_plans.basico.price_per_doc`, hoy US$ 0,20) y regalo de alta (`billing_plans.free.balance_usd`, US$ 5). `SECURITY DEFINER`, sólo lectura, **`EXECUTE` para `anon` a propósito** (son precios públicos; sin dólar, sin pesos, sin datos de nadie); anon sigue sin leer las tablas de precios ni `quote_purchase`. `LandingPage.tsx` la lee al abrir y arma precio, «Acreditás USD X · +N %», «hasta ~N documentos» (recibe ÷ precio base: en la landing nunca hay usuario con configuración) y el cartel «Bonus desde USD …»; si la base no responde, quedan los valores fijos de `PLANES` (puestos al día al 19/09). Debajo aclara que integraciones y extras se cobran aparte. Validado: el director puso el Profesional en 12 % y la landing mostró «Acreditás USD 112 · +12 %»; volvió a 10 (18:23 UTC). **El saldo no vence** (decisión del director; verificado que no existe ningún mecanismo de vencimiento): la pregunta frecuente decía «caduca tras 6 meses de inactividad» y se corrigió. Vuelta atrás de la función: `DROP FUNCTION public.get_public_pricing();` (la landing cae a los valores fijos).
- **Funciones viejas de Mercado Pago apagadas (BILLING-COMPRA-5.6, 2026-09-20; lo levantó el agente de Ámono):** en Supabase seguían encendidas dos edge functions de junio que no usa ninguna pantalla ni el servidor (grep en `src/` y `worker/`: cero llamadas a `functions/v1`; sin uso en los registros de las 24 h previas): `create-payment-preference` (cobraba los precios en pesos de `billing_plans`; la podía llamar cualquier usuario logueado) y `mp-webhook` (receptor de avisos abierto: con un pago nuevo lo marcaba `approved` pisándole `metadata` **sin acreditar** —y la revisión automática sólo mira `pending`—; con el camino viejo acreditaba `docs_included` como si fueran dólares). Se desplegaron versiones apagadas por el MCP de Supabase: `create-payment-preference` **v14** (`verify_jwt` true) responde **410** «Fuera de servicio…»; `mp-webhook` **v6** (`verify_jwt` false) responde **200 «OK» sin tocar la base** y deja `mp-webhook apagada: aviso recibido y descartado {method,type,data_id}` en los registros (así se sabe si el panel de Mercado Pago todavía le manda avisos). Fuente de lo desplegado: `supabase/functions/<nombre>/index.ts`; originales (v13 y v5) en `supabase/functions/_legacy/` con la vuelta atrás (volver a desplegar el original con el mismo `verify_jwt`). Antes de apagar se verificó que los 9 pagos del camino viejo son de Aignition, `pending` y sin `gateway_payment_id` (pruebas del director): nadie queda sin acreditar.
- `POST /api/mp/webhook` (gateway.mjs) — IPN validado e2e **sólo en sandbox (junio)**. No tocar lookup `preference_id`/`external_reference` ni idempotencia por `gateway_payment_id`.
- `create-preference`/`create-custom-preference` — generan UUID antes del INSERT y pasan `external_reference`. No cambiar el orden.
- RPC `add_credits(...)` — SECURITY DEFINER, solo desde gateway con service key, nunca frontend.
- `MP_ACCESS_TOKEN` — producción (`seller_id 290523599`). No usar sandbox.
- RPC `charge_credit(...)` — costo = `(base + features activas + polling) × docs`. Base = `billing_plans.price_per_doc` del plan `basico` (COALESCE $0.30). **Cambiar precio base = editar ese campo desde MonitoringPage → Precios** (efecto inmediato sin redeploy). Acepta `p_polling_interval_minutes` → `polling_interval_tiers.cost_per_doc`.
- `polling_interval_tiers` — 12 tramos (1-120 min), `active` controla visibilidad; editable desde MonitoringPage→Precios vía RPC `update_polling_tier` (superadmin). No hardcodear.

### Google Drive OAuth
- `VITE_GOOGLE_CLIENT_ID=59795666065-qhm5r5p4q9rj8glpauhir6a6r4uen4sj.apps.googleusercontent.com` (identifica la app, no al tenant).
- `VITE_GOOGLE_REDIRECT_URI=https://api.agoradigital.io/api/auth/google/callback` — debe coincidir con el `.env` del worker (`GOOGLE_REDIRECT_URI`) y Google Cloud Console. (Migrado de dataland.aignition.net el 2026-06-28, GTM-04.)
- `integration-poller.mjs` encola con `${gatewayUrl}/api/enqueue`. `worker.mjs:GATEWAY_URL` = base sin path (`https://api.agoradigital.io`), no agregar `/api/enqueue`.

### Seguridad / DB
- `tenant_integrations.credentials` **NO existe** — están en `credentials_encrypted` (bytea). Leer vía RPC `admin_get_integration_credentials(p_integration_id, p_org_id)`. En REST seleccionar `integration_type, folder_path, organization_id` (nunca `credentials`).
- `integration_processed_files` — RLS ON (políticas `tenant_select_own`/`tenant_insert_own`). No deshabilitar.
- Trigger `on_auth_user_created → handle_new_user()` crea org+profile automáticamente. El frontend **nunca** inserta org/profile.
- RPCs SECURITY DEFINER (`charge_credit`, `add_credits`, `get_all_tenants_admin`, `get_tenant_jobs_admin`, `approve_document_row`) — no eliminar/modificar sin evaluar RLS. ⚠️ En RPCs con `RETURNS TABLE(id ...)`, calificar SIEMPRE las columnas en subqueries internas (`profiles.id = auth.uid()`) o Postgres lanza `42702 ambiguous` (bug de TASK-95).
- `public.profiles` (INC-003): `anon` sin INSERT/UPDATE; `authenticated` solo UPDATE sobre `full_name` y `onboarding_completed`. NO devolver grants de tabla ni sumar columnas sensibles al GRANT. Escribir `organization_id`/`role`/`is_superadmin` = función SECURITY DEFINER con guard, nunca desde el cliente.
- **Antes de borrar una política de RLS/storage, listar qué caminos del producto dependen de ella.** «No tiene tráfico» no es «no se usa»: es «todavía no se notó». El 2026-09-10 se borró `facturas_public_select` para cerrar la enumeración del bucket y con ella se rompió la carga web, que la usaba para **leer antes de escribir** (`upsert:true`); nadie lo notó durante dos días porque esa vía no tenía tráfico desde el 13 de agosto.

### Realtime
- Publicación `supabase_realtime`: `pdf_jobs` y `pdf_job_rows` habilitadas (migración `enable_realtime_pdf_jobs`). No remover.
- Nombres de canal — NO duplicar: `useClientJobs`→`'pdf_jobs_changes'`; `usePdfJobs`→`'mis_procesos_jobs_changes'`+`'mis_procesos_rows_changes'`. Dos hooks con el mismo nombre corrompen la subscripción.
- Polling de respaldo (8s/5s) activa solo con jobs `pending`/`processing`; depende de Realtime para el INSERT inicial.

### Período de jobs (la DB es la única fuente)
- Trigger `trg_set_pdf_job_period` (BEFORE INSERT en `pdf_jobs`) setea `period_month`/`period_year` desde `created_at` en horario `America/Argentina/Buenos_Aires`, **override siempre**, universal (manual/drive/supabase/firebase). Período = mes de **procesamiento** (base de consumo por mes). NO toca `pdf_job_rows.fecha` (dato del OCR). El frontend ya NO envía período. No volver a setearlo desde frontend/gateway.

### Observabilidad
- Sentry (`@sentry/react` + vite-plugin). El `SENTRY_AUTH_TOKEN` va en el `.env` local y **`vite.config.ts` lo lee con `loadEnv`**: NO hace falta exportarlo en la shell antes de compilar (ese ritual era justamente el bug — con `process.env` a secas el token estaba puesto y no se usaba). Con token: mapas `'hidden'`, se suben a Sentry y se borran de `dist/`. Sin token: no se generan y el build lo avisa por consola.
- `GET /api/metrics` (gateway) proxy a `metrics.mjs:9090`, consumido por MonitoringPage. No cambiar ruta/auth.

### Deploy (reglas)
- Worker: SIEMPRE `docker compose build && docker compose up -d --force-recreate` desde `/root/worker/`. NUNCA `docker run` manual (queda en `caddy_net` y Caddy no alcanza `:3001`).
- Frontend: `npm run build` → `rm -rf /var/www/dataland/assets` → `scp -r dist/.`. NUNCA Netlify ni git pull en el server.
- `VITE_WORKER_GATEWAY_URL` = base SIN path (cada archivo appenda su endpoint).
- **Un comando interrumpido puede haber corrido igual** (lección del 2026-09-19): si la herramienta devuelve «interrumpido» o «rechazado» después de que el comando arrancó, el servidor lo termina igual. Antes de reportar que algo no corrió, verificarlo en el servidor o en la base. Ese día un bloque de pruebas se reportó como no ejecutado y la base mostraba que sí.
- **Verificar un deploy de frontend por CONTENIDO, nunca por código de estado.** Caddy sirve la SPA con *catch-all*: cualquier ruta inexistente devuelve **200 con el `index.html`**, no 404 — un `.map` borrado, un archivo inventado y un respaldo no expuesto dan los tres 200. Comparar el **cuerpo** (y el `content-type`) contra el de una ruta inventada: `text/html` + mismo tamaño ⇒ no está; `"version":3` ⇒ el mapa SÍ se sirve. Detalle y comandos en `RUNBOOK-ROLLBACK.md` → «Trampa del catch-all». (Hallado el 2026-09-12 verificando que los mapas dejaran de publicarse: leído por el código de estado, el resultado decía exactamente lo contrario de la realidad.)
- **Respaldo obligatorio antes de un deploy de frontend**: `cp -a /var/www/dataland /var/www/dataland.bak-$(date +%Y%m%d-%H%M%S)`. El deploy arranca con `rm -rf` y no deja copia; sin respaldo la única vuelta atrás es recompilar desde git. Procedimiento completo en `RUNBOOK-ROLLBACK.md` (sección de frontend, agregada el 2026-09-12).
- **`vite.config.ts` lee las variables con `loadEnv`, nunca con `process.env` a secas** — Vite NO expone el `.env` en `process.env` dentro del archivo de configuración. Con `process.env` el token del `.env` está puesto y no se usa, en silencio.

### Pipeline de integración (worker) — CERRADO
Validado y estabilizado. No tocar sin tarea explícita.
- `poller-handoff.mjs` — central compartido: upload a Aurora Storage, enqueue. **Dedup por SHA256/drive_file_id RETIRADO (DEC-019, 2026-06-24)**: los pollers ya NO deduplican; levantan y procesan todo lo que haya (el usuario es responsable de lo que sube). Garantía de procesamiento único = el archivo **sale de la raíz** (move a `en_proceso/`); por eso ahora **se encola SOLO si ese move tuvo éxito** (si no, no se encola → no se recobra). Las copias en `en_proceso/procesados/fallidos` del cliente llevan **prefijo timestamp** (`{Date.now()}_{nombre}`) para que nombres repetidos no choquen (fix 2026-06-24; Supabase/Firebase. Drive mueve por ID, no aplica). El `original_filename` del documento conserva el nombre real; solo la copia archivada lleva el prefijo. Toda integración nueva lo usa, no duplicar. **SANITIZE-FILENAME (2026-07-15):** en `poller-handoff.mjs:uploadAndEnqueue` el nombre del archivo se **sanea** para la clave de storage de Aurora (`safeName = filename.replace(/\s+/g,'_').replace(/[^\w.\-]/g,'_')`) ANTES de armar `uniqueName`/`storagePath`/`fileUrl`. Motivo: un nombre con **espacios** (o `#`, acentos, paréntesis, etc.) genera una URL pública inválida y Mistral OCR devuelve `400 File could not be fetched` → el job falla (caso real Menara `ELECT. RAF. 15207.PDF`). Bug preexistente, afecta a TODAS las integraciones. El nombre REAL se conserva en `original_filename` (display); solo cambia la ruta interna de Aurora. **POLLER-BALANCE-GUARD (2026-08-07, K2-11):** `MIN_PROCESSING_BALANCE=1` (mismo umbral que el 402 de `gateway.mjs:handleEnqueue` — cambiarlo = tocar ambos lados en tándem) + `orgHasProcessingCredits` (fail-open); `runIntegrationPoller` skipea la integración SIN tocar archivos si saldo <$1 (`poller.skip_no_credits` 1×/ciclo); `uploadAndEnqueue`/`enqueueJob` tipan el 402 y cada poller devuelve el archivo a la raíz y corta el loop.
- `integration-file-mover.mjs` — mueve `en_proceso/`→`procesados/`/`fallidos/` post-worker (llamado desde worker.mjs). Credenciales vía `credentials_encrypted` + RPC desencriptar. **FILE-RENAME-BY-DATA (Fase 1, 2026-06-26):** al ir a `procesados/`, si `worker.mjs` pasa `renameBase` (storage Supabase/Firebase, job de 1 doc con los 3 datos), renombra el archivo a `{cuit}_{numero}_{codigo_afip}.{ext}` (helper compartido `doc-naming.mjs:buildDocFileBase`). Drive NO se renombra (queda como está). Duplicado (destino ya existe) → no pisa: mueve con sufijo `__DUPLICADO_{ts}` + log `file_mover.duplicate`.
- `gateway.mjs` — rutas, billing, IPN, VALID_SOURCES. `metadata` hace spread del body (`...(body?.metadata ?? {})`) para preservar `fileMeta` de pollers (`integration_id`, etc.); sin eso file-mover no mueve.
- `document-processor.mjs` (OCR+IA), `worker.mjs` (BullMQ + cron), `supabase-storage-poller.mjs`, `firebase-storage-poller.mjs`, `output-depositor.mjs` (CSV/XLSX → `extracciones/`). `integration-poller.mjs` (Drive, prod) — tocar SOLO para TASK-96.
- **Extracción de adjuntos embebidos** (pdfdetach+mutool+PyMuPDF en `zip-processor.mjs`, vía ZIP y PDF suelto) está **gateada por org** (TASK-108): corre solo si `tenant_feature_flags.extract_embedded_attachments=true` para esa organización (default false). El worker lee el flag con `getExtractAttachmentsFlag` (`worker.mjs`) y lo pasa a `processZip`; OFF → `pdfFiles=[]` → 0 OCs, no se cobran adjuntos. Hoy ON solo Aignition.
- **Estructura de carpetas uniforme**: usuario suelta en raíz/carpeta → poller mueve a `en_proceso/` → worker a `procesados/`/`fallidos/` → output a `extracciones/`.
- **Agregar integración** = `worker/{nombre}-poller.mjs` (list+download+move a `en_proceso/` + `uploadAndEnqueue` con `fileMeta`) + `input_source` al CHECK de `pdf_jobs` y a `VALID_SOURCES`. El movimiento post-worker es automático.
- **Archivos rechazados** (formato no soportado): cada poller llama `registerRejectedFile` (poller-handoff) → RPC `gateway_register_rejected_file` crea un `pdf_jobs` `status='error'`, `error_type='rejected'` con la razón en `error_message`, y mueve el archivo a `{cliente}/fallidos/`. Universal, no se cobra, el front lo muestra como Fallido (TASK-110).

---

## Decisiones clave (fuente: Decisions Log en Notion)

- **DEC-007 (enmendado) + DEC-017** — DB vs worker: *procesamiento/lógica compleja* (OCR, IA, parseo, merge OCs, cálculos, deps externas o multi-entidad) → **Worker**. *Derivación determinística sobre una sola fila* (ej. `doc_status` vía `classify_pdf_job_row`; conteos vía `trg_sync_job_counts_rows`) → **aceptable en DB**. La clasificación de `doc_status` se mantiene en el trigger por escala; migrar al worker SOLO al cruzar gatillos de DEC-017 (deps externas tipo AFIP/histórico/comparación entre docs; o CPU>70% / p95 degradada / millones de filas / límites del plan). Tests: **TEST-CLASSIFY-TRIGGER** ✅ (16 casos, `sql/tests/test_classify_pdf_job_row.sql` + func `test_classify_pdf_job_row()` en DB; correr `SELECT * FROM test_classify_pdf_job_row();` tras cualquier cambio del trigger).
- **DEC-011** — N8N eliminado del pipeline. No existe, no referenciar.
- **DEC-018** — Regionalización multi-país = escalado futuro: país como dimensión de primer nivel (prompt + `document_types.country` + modelo de campos genérico tipo `tax_id`); prompt-por-país y prompt-en-DB se hacen dentro de ese épico. Hoy solo AR. Seguimiento: Kanban TASK-113.
- **DEC-022 (2026-09-10)** — **Un solo bucket de almacenamiento: `documents`.** `facturas` queda congelado, público y con sus archivos intactos. Había dos armarios para lo mismo: el poller de Drive escribía en `facturas` y el resto en `documents`. Dos superficies para asegurar en vez de una. Congelar en vez de migrar evita mover 1.347 archivos y romper las URLs ya guardadas. **Estado al 2026-09-12: el congelamiento está COMPLETO del lado de las ESCRITURAS.** El worker dejó de escribir en `facturas` el 2026-09-10 (commit b588d7a: poller de Drive + `STORAGE_BUCKET=documents` para los PDFs extraídos de ZIP), y la **carga manual desde la app** (`SubirZipPage` → `src/lib/pdfJobHelpers.ts:uploadFileToWorker`) el 2026-09-12: ahora sube a `documents/{org_id}/uploads/{jobId}.{ext}` con `upsert:false` y URL firmada, apoyada en las políticas `documents_insert_propia`/`documents_select_propia`. **Nada escribe ya en `facturas`.** Queda pendiente borrar sus dos políticas de escritura (ver Problemas conocidos) y, aparte, firmar los cuatro lugares del worker para poder cerrar los buckets.

## Incidentes de seguridad

- **INC-001 (2026-09-10) — Inyección de comandos en el worker por nombre de archivo.**
  - **Qué era:** los nombres de archivo llegaban a `/bin/sh` sin escapar. `zip-processor.mjs` (`runCmd`) y `worker.mjs` usaban `child_process.exec` con templates de JS interpolados; las comillas dobles NO protegen (dentro de comillas dobles la shell igual expande `$(...)` y las comillas invertidas). Un archivo llamado p.ej. `factura$(comando).pdf` ejecutaba `comando` en el contenedor.
  - **Por dónde entraba:** (1) nombres de archivo dentro de un ZIP/RAR — **cualquier organización, cualquier canal (Drive/storage/panel), sin depender de ningún flag** (el path de conversión de escaneados `pdftotext`/`pdftoppm`/`mv` corre para todos); (2) nombres de adjuntos embebidos dentro de un PDF — organizaciones con `extract_embedded_attachments=true`; (3) el `file_url` que llega por `/api/enqueue`.
  - **Quién estaba expuesto:** **MENARA CONSTRUCCIONES S.A.** y **Aignition** tienen `extract_embedded_attachments` encendido, y sus documentos entran por su bucket — o sea PDFs que les mandan sus proveedores. Era el camino de producción, no un caso de laboratorio. La vía del ZIP (sin flag) afectaba a todos los tenants.
  - **Qué había en el contenedor:** `SUPABASE_SERVICE_KEY` (saltea el RLS de los dos productos), `MP_ACCESS_TOKEN` de producción, claves de OpenAI y Mistral, secreto de OAuth de Google, contraseña de Redis.
  - **Desde cuándo:** el patrón `exec`+interpolación entró con el ZIP processor el **2026-05-29** (commit `73142ce`, TASK-35, worker v0.6.0) y se replicó en el path de adjuntos individuales de `worker.mjs`.
  - **NO se probó en producción:** el hallazgo sale de leer el código. La reproducción se hizo en local, aislada, con un A/B `exec` (vía `/bin/sh`, igual que Alpine) vs `execFile`: el patrón viejo ejecutó el payload y creó un archivo marcador; el nuevo recibió cada nombre como literal.
  - **Arreglo:** commit `7be8026` (rama `fix/inyeccion-comandos-worker`, desplegado el 2026-09-10, commits `7be8026`+`7a2bc0e`+`bfd5ae6`). Se reemplazó `exec` por `execFile` con argumentos en arreglo (patrón de `document-processor.mjs:309`); `cd X &&` → `{ cwd: X }`; los `find`/`for` de shell → JS (`walkFiles`/`walkDirs` + `rename`/`rmdir`); `wget` → `fetch`+`writeFile`. Validado: `processZip` REAL en imagen Alpine (0 markers, ambos docs con nombre literal) + tras el deploy, job real de Aignition a `done`.
  - **REGLA (queda):** en este repositorio **no se usa `exec`**. Binario + argumentos en arreglo, siempre. El `file_url` de `/api/enqueue` además se restringe al host de Supabase (commit `7a2bc0e`).
  - **Nota operativa (memoria):** la descarga pasó de `wget` (que escribía a disco a medida que bajaba) a `fetch`+`writeFile`, que carga el archivo entero en memoria y lo duplica al copiarlo: ~100 MB por trabajo con un ZIP de 50 MB, y con concurrencia 3 son ~300 MB de pico transitorio en un droplet de 1,9 GB. Entra, y se libera al escribir. Si algún día molesta, la salida es streamear: `Readable.fromWeb(res.body)` → `createWriteStream`.
  - **Validado en PRODUCCIÓN (2026-09-10):** ZIP con payload real por Drive de Aignition — job `05870879-2bfb-4146-b3a3-b1da9004ef13`, 14:59:13→14:59:40, 2 docs, 0 fallidos; manifiesto con los nombres **literales** `factura$(touch PWNED_A).pdf` y ``nota`touch PWNED_B`.pdf`` → processed. Búsqueda de marcadores en TODO el FS del contenedor (`find / -name "PWNED_*"`) → **vacío**: ningún comando se ejecutó. Paso 7 cerrado. Además, Menara procesó 7 trabajos a las 14:47 (todos `done`) con el código nuevo — el tenant que factura ya pasó tráfico real por el arreglo.
- **INC-002 (2026-09-10) — La llave del gateway está publicada en el bundle del front: cualquiera encola a nombre de cualquier organización.** Estado: **PENDIENTE**. Verificado contra el sitio en vivo (2026-09-10), no solo sobre el código.
  - **Qué es:** `staging-key-2026` está en texto plano dentro del bundle desplegado (`main-POGKC2yf.js` y `SubirZipPage-B63UJkcz.js`). Es el valor real de `VITE_WORKER_API_KEY`: las variables `VITE_` se compilan adentro del JavaScript que descarga cualquiera. Además es el valor de reserva en el código (`pdfJobHelpers.ts:73-74`, `?? 'staging-key-2026'`): queda pegada aunque la variable falte al compilar.
  - **Qué abre:** es la misma `GATEWAY_API_KEY` del servidor, y protege TODAS las rutas del gateway menos el aviso de MercadoPago (`gateway.mjs:1217-1227`) y el callback de OAuth de Google (`:1206-1215`, exento a propósito). Con ella:
    - `/api/enqueue` toma el `organization_id` **del cuerpo del pedido** sin comprobar que quien llama pertenezca a esa organización (`gateway.mjs:813-825`) → encolar trabajos a nombre de cualquier organización, gastándole el saldo.
    - `/api/prompt` devuelve el prompt de extracción completo, sin necesitar ningún otro dato.
  - **Agravante — los mapas de código están publicados:** se descargó desde `agoradigital.io/assets/SubirZipPage-B63UJkcz.js.map`, sin sesión, el código fuente TypeScript original de `pdfJobHelpers.ts` y `SubirZipPage.tsx`, sin minificar y con comentarios. Causa: `vite.config.ts:26` genera `sourcemap: true` siempre, y solo se borran de `dist/` si el build corre con `SENTRY_AUTH_TOKEN` presente (`filesToDeleteAfterUpload`, líneas 6-18). Build sin la variable → los mapas viajan al servidor con el `scp`.
  - **Visto además en el código (2026-09-11, sin probar en vivo):** (a) `gateway.mjs:1230` — `if (GATEWAY_API_KEY) { ... }`: si la variable falta del `.env`, el gateway no le pide autenticación a nadie (falla abierto). (b) Las rutas de MercadoPago `create-preference`/`create-custom-preference` toman el `user_id` del cuerpo y de ahí resuelven la organización (`gateway.mjs:929-945`, `:1009`): tampoco verifican quién llama.
  - **Dónde apareció:** en `PROPUESTA-URLS-FIRMADAS-AGORA.md` §6, buscando el quinto lugar donde se arma la URL pública (`pdfJobHelpers.ts`); la propuesta lo dejó como «a confirmar» y se confirmó contra el sitio en vivo el 2026-09-10.
  - **ROTAR LA LLAVE NO SIRVE:** la nueva vuelve a quedar dentro del bundle siguiente. **Arreglo:** que el gateway valide el JWT de Supabase del usuario y saque el `organization_id` del token, no del cuerpo. Es un cambio de fondo y necesita su propio plan: el front llama con sesión de usuario, pero los pollers del worker también encolan por `/api/enqueue` (`integration-poller.mjs`) y no tienen sesión — hay que separar los dos caminos.
- **INC-003 (2026-09-11) — `profiles` con columnas sensibles escribibles por el cliente: salto de inquilino con permisos de dueño.** Estado: **APLICADO (2026-09-11)**.
  - **Qué era:** `anon` y `authenticated` tenían INSERT y UPDATE sobre TODAS las columnas de `public.profiles`, incluidas `is_superadmin`, `role` y `organization_id`. Las políticas `"Profiles - update self"` y `profile_self_update` solo exigen `id = auth.uid()`, en USING y en WITH CHECK; nada sobre las demás columnas.
  - **La cadena:** (1) `current_org_id()` devuelve `profiles.organization_id` del propio usuario. (2) Los predicados de dueño preguntan solo «¿existe un perfil con mi id y `role='owner'`?», SIN comprobar de qué organización. (3) Consecuencia: con un UPDATE sobre su propia fila, cualquier usuario con sesión se pone `role='owner'` y `organization_id=<víctima>`, y obtiene sobre ese inquilino: `organizations` (select/update/**DELETE**), `pdf_jobs` (select/update/delete) y `profiles` (select/update).
  - **Severidad: la más alta de la serie.** No es escalar a superadministrador — `is_superadmin` hoy no lo lee ninguna política —, es **saltar de inquilino con permisos de dueño**. El registro está abierto, así que el atacante solo necesita crearse una cuenta.
  - **Quién lo encontró:** el agente de Amono, preparando una función propia de superusuario. Tercer hallazgo de la misma familia en dos días (vista sin `security_invoker` → buckets públicos → esto). Los tres son «algo que el cliente alcanza y no debería».
  - **Cómo se aplicó (y el tropiezo):** el primer intento fue por columna (`revoke update/insert (is_superadmin, role, organization_id) ... from anon, authenticated`) y **NO TUVO EFECTO**: los permisos eran de tabla (`anon=arwdDxtm`, `authenticated=arwdDxtm`, sin ningún `attacl`), y un REVOKE por columna contra un GRANT de tabla es un **no-op silencioso** en Postgres. Lo que funcionó:
    ```sql
    REVOKE UPDATE, INSERT ON public.profiles FROM anon, authenticated;
    GRANT  UPDATE (full_name, onboarding_completed) ON public.profiles TO authenticated;
    ```
    Estado final verificado: `authenticated` solo tiene UPDATE sobre `full_name` y `onboarding_completed`; `anon` no tiene ni UPDATE ni INSERT. **Vuelta atrás:** `GRANT UPDATE, INSERT ON public.profiles TO anon, authenticated;`
  - **Por qué no rompe nada (verificado función por función):** `handle_new_user`, `set_user_superadmin` y `current_org_id` son las tres SECURITY DEFINER → el alta, el interruptor de superadmin (`MonitoringPage.tsx:605`) y la resolución de organización no usan los permisos del cliente. En `src/` la única escritura sobre `profiles` es el INSERT `{id, organization_id}` de `AuthContext.tsx:105` (`createOrgPostConfirmation`, resto del registro previo a `handle_new_user`): ya fallaba siempre por clave primaria (el trigger crea el perfil en el `signUp`); ahora falla por permisos. Sin cambio visible. No existe pantalla que cree o edite perfiles de empleados (la política `profiles_insert_to_org_owner` no tiene quien la use desde el front). El gateway lee `profiles.organization_id` con service key (`gateway.mjs:939` y `:1009`, rutas de MercadoPago): no lo afecta, y es otro consumidor que confía en que esa columna no la escribe el cliente.
  - **REGLA (queda):** un REVOKE por columna no recorta un GRANT de tabla. Primero se quita el permiso de tabla, después se devuelven las columnas. Y el resultado se verifica mirando los permisos, no porque el comando no dio error.
  - **Deuda que queda abierta:** los predicados de dueño no verifican de qué organización sos dueño (fila aparte en Problemas conocidos).
- **INC-004 (2026-09-12) — `admin_get_org_clients` sin control de quién la llama: la lista de clientes de cualquier organización, con sesión anónima.** Estado: **CERRADO** (2026-09-12).
  - **Qué es:** en el esquema `public` hay **siete** funciones `admin_*` — no trece, verificado en el catálogo (`pg_proc.proacl`, no `information_schema`). Seis están en `plpgsql` y verifican `service_role` al entrar, levantando excepción si no. **`admin_get_org_clients(uuid)` es la única en lenguaje `sql` y no tiene ningún control**: recibe un `organization_id` y devuelve los clientes de esa organización sin mirar quién pregunta.
  - **Por dónde se alcanzaba:** las **siete** tenían EXECUTE para **PUBLIC** (`=X` en `proacl`), además de `anon` y `authenticated`. PostgREST publica las funciones de `public` como `/rest/v1/rpc/…`, así que con la llave publicable y un `organization_id` se leía la tabla `clients` de cualquier inquilino.
  - **TRAMPA — misma familia que INC-003:** el arreglo que se propuso primero (`REVOKE … FROM anon, authenticated`) **NO habría servido**: con el GRANT a PUBLIC intacto, `anon` hereda el permiso igual. Es el mismo error de forma que el REVOKE por columna contra un GRANT de tabla.
  - **REGLA (queda):** revocar desde **la capa que concede** — PUBLIC antes que los roles — y **volver a leer el catálogo** después de aplicar. Siempre. Que el comando no dé error no prueba nada.
  - **Paso 1, APLICADO (2026-09-12):** `REVOKE EXECUTE ON FUNCTION public.admin_get_org_clients(uuid) FROM PUBLIC, anon;` → `proacl` reverificado: `{postgres=X, authenticated=X, service_role=X}`.
  - **Severidad real, sin inflar:** `clients` tiene **3 filas**; una es Menara con su CUIT, que es dato semi-público. Y hace falta conocer un `organization_id`, que no se adivina. Agujero real, poco detrás hoy, **crece con cada cliente nuevo**.
  - **Reconocimiento del código (2026-09-12, grep sobre TODO el repo: `src/`, `worker/`, `supabase/`, `sql/`):** las siete se llaman **solo desde el worker y siempre con `SUPABASE_SERVICE_KEY`**. **Cero llamadas desde el frontend** (en `src/` no aparece ninguna `admin_*`; la única RPC con «admin» en el nombre es `get_admin_jobs`, otra función, con guard de superadmin) y **cero edge functions** (`supabase/` solo tiene migraciones). Por eso el paso 2 es seguro. Detalle: `admin_get_org_clients` → `gateway.mjs:156`, `integration-poller.mjs:397`. `admin_get_active_integrations` → `integration-poller.mjs:324`, `ftp-sftp-poller.mjs:345`, `poller-handoff.mjs:242`. `admin_get_integration_credentials` → `gateway.mjs:288,341,408,507,693`, `integration-file-mover.mjs:44`. `admin_update_integration_credentials` → `gateway.mjs:231,249,357`. `admin_get_output_integration` → `output-depositor.mjs:591,824`. `admin_register_processed_file` → `poller-handoff.mjs:98`, `ftp-sftp-poller.mjs:162,282`. `admin_update_last_polled` → `integration-poller.mjs:351,358,372,390,570,588`, `ftp-sftp-poller.mjs:369,381`, `poller-handoff.mjs:267,274,280`. Los cuatro helpers que las invocan (`callSupabaseRpc` en gateway, `callRpc` en poller-handoff, `supabaseFetch` en output-depositor y el `fetch` directo de integration-file-mover) mandan `SUPABASE_SERVICE_KEY` en `apikey` y `Authorization`.
  - **Paso 2, APLICADO (2026-09-12) — INC-004 CERRADO.** Todo verificado en `pg_proc`, no por ausencia de error: (a) las **siete** quedaron con `proacl {postgres=X, service_role=X}` — **PUBLIC, `anon` y `authenticated` fuera de las siete**; (b) `admin_get_org_clients` **reescrita en `plpgsql`** con el mismo candado de `service_role` que sus hermanas — copiado **literal** de `admin_get_output_integration`, no inventado; (c) `search_path` fijado en las tres que lo tenían en NULL (`admin_get_org_clients`, `admin_get_integration_credentials`, `admin_update_integration_credentials`) → ahora lo tienen **las siete**.
  - **Validación POR COMPORTAMIENTO (2026-09-12):** los pollers llaman **cada minuto** a `admin_get_active_integrations` y `admin_update_last_polled`, dos de las siete cerradas. `last_polled_at` **siguió avanzando después del cambio** (08:17 → 08:19 en las tres integraciones activas): el worker no se rompió. Es la prueba que vale — el service key sigue entrando, el resto no.
  - **Vuelta atrás:** `GRANT EXECUTE … TO authenticated` en las siete, y restaurar `admin_get_org_clients` a su versión en lenguaje `sql` sin candado (queda en el historial de esta ficha).
  - **Quién lo encontró:** el agente de Ámono. **Cuarta vez en tres días**, misma familia: «algo que el cliente alcanza y no debería» (vista sin `security_invoker` → buckets públicos → INC-003 → esto).
- **INC-005 (2026-09-12) — puertos de servicio del droplet abiertos a internet: métricas sin autenticación y un extractor que cualquiera puede tocar.** Estado: **CERRADO — cortafuegos aplicado y verificado (2026-09-12)**; los servicios **siguen escuchando**, lo que cambió es quién llega.
  - **Qué es:** `dl-worker` y `extractor` corren en **modo de red `host`**, así que sus puertos quedan en **todas** las interfaces sin figurar como «publicados» en `docker ps` (por eso no saltaban a la vista). Medido desde afuera el **2026-09-12 08:28-08:31 UTC**, antes de aplicar nada, los siete puertos contestaban desde internet: 22, 80, 443, **3001, 9090, 5679 y 2376**.
  - **9090 — métricas de la cola servidas a internet SIN autenticación.** No es teoría: un `GET http://157.230.231.207:9090/metrics` sin credencial devolvió **HTTP 200** con `worker_version v1.9.9`, nombre de la cola, profundidad (waiting/active/delayed), **totales 2.241 completados / 21 fallidos**, latencias **p50 19.678 ms / p95 39.580 ms / avg 22.391 ms** y `error_rate_pct`. **En esta misma bitácora decía que `/api/metrics` va con Bearer** — y es cierto, pero esa protección **se saltea pegándole directo al 9090**, que es el puerto al que el gateway hace de proxy. La puerta con llave tenía al lado una ventana abierta. No hay datos de clientes ahí, sí el pulso operativo completo de la plataforma.
  - **5679 — `extractor` sin autenticación, y con internet tocándole la puerta hace meses.** Contesta `{"error":"Unexpected end of JSON input"}` a un GET pelado: parsea cuerpos **sin nada de autenticación adelante**. Su log acumula **333 entradas de ese mismo error de sondeo**, **9 en las últimas 24 h — de las cuales 2 fueron nuestras** (los probes de 08:30), **7 ajenas**. **CERO extracciones exitosas en todo el registro: no hay ninguna evidencia de que alguien lo use.** Contenedor `root-extractor`, arriba hace 3 meses, y **no aparece ni en `/root/worker/.env` ni en ningún `.mjs` del worker** — el worker no lo llama. Con el puerto 5679 (el alterno de n8n) y n8n dado de baja (GTM-07), huele a resto de la mudanza.
  - **2376 — API de Docker, exige certificado de cliente.** El handshake TLS se rechaza sin certificado, así que **no era una raíz abierta**. Se cierra igual: la API de Docker no tiene nada que hacer de cara a internet.
  - **3001 — el cortafuegos NO arregla INC-002.** Contesta 401, pero la llave que pide está publicada en el bundle (INC-002) y el camino bueno sigue vivo: `api.agoradigital.io:443` → Caddy → `172.20.0.1:3001`. Cerrar 3001 en la IP pública **saca una puerta duplicada, no la puerta**. Que nadie lea esto como que INC-002 bajó de prioridad.
  - **Por qué cortafuegos de nube y no `ufw`:** con puertos publicados por Docker, `ufw` **no los cierra** (Docker escribe sus propias reglas y saltea la cadena INPUT). El de DigitalOcean filtra **antes** de llegar al droplet, así que sí los cierra. `ufw` estaba **inactivo**: no había ninguna capa que filtrara, ni conflicto que resolver.
  - **Por qué no rompe a Caddy (verificado ANTES de aplicar):** Caddy proxea a **`172.20.0.1:3001`**, que es el gateway del puente `n8n_default` (subred 172.20.0.0/16, confirmado con `docker network inspect`). Ese tráfico va contenedor → puente → host y **nunca toca la interfaz pública**, que es lo único que filtra el cortafuegos.
  - **Cierra sin apagar nada — y eso es a propósito:** los procesos siguen escuchando, solo deja de llegarles tráfico de internet. Si resultara que alguien los usaba, **se rompe de forma visible y se reabre con un clic** (desasociar el cortafuegos del droplet, inmediato). Es lo contrario de apagar el contenedor, que rompería igual pero sin vuelta atrás barata.
  - **⚠️ EL DROPLET DEJA DE RESPONDER AL PING, Y ES ESPERADO.** La lista de entrada son **solo 22/80/443**: ICMP no está en la lista, así que `ping 157.230.231.207` pasa a no contestar. **No es un servidor caído.** Queda escrito acá a propósito, antes de aplicar, para que nadie pierda media hora diagnosticándolo. La señal de que el droplet está vivo es `https://agoradigital.io` cargando y `https://api.agoradigital.io/health` devolviendo 401 — no el ping.
  - **LAS REGLAS QUE QUEDARON — cortafuegos `agora-prod`, asociado al droplet `droplet-ubuntu-do`, 6 reglas.** **Entrada (3):** TCP **22**, TCP **80**, TCP **443**, las tres desde `0.0.0.0/0` y `::/0`. Todo lo demás de entrada se rechaza. **Salida (3):** **ICMP**, **TCP 1-65535** y **UDP 1-65535**, hacia `0.0.0.0/0` y `::/0` — o sea **TODA la salida abierta, y eso no es opcional**: DigitalOcean, al crear un cortafuegos, **deniega la salida que no esté listada**; si ese bloque queda vacío se caen Redis Cloud (16705), Supabase, OpenAI, Mistral, Google, MercadoPago y la renovación de certificados. Es el error clásico y es total.
  - **VERIFICADO — los siete puertos, antes y después, mismo método** (prueba TCP real; el probe con `telnet://` da **falsos timeouts hasta en el 443** y no sirve para esto):

| Puerto | Qué es | ANTES (08:28 UTC) | DESPUÉS (09:25 UTC) |
|---|---|---|---|
| 22 | sshd | ABIERTO | **ABIERTO** — queda |
| 80 | docker-proxy → Caddy | ABIERTO | **ABIERTO** — queda |
| 443 | docker-proxy → Caddy | ABIERTO | **ABIERTO** — queda |
| 3001 | gateway (worker, host mode) | ABIERTO → 401 | **sin respuesta** ✅ |
| 9090 | métricas (worker) | ABIERTO → 200 con datos | **sin respuesta** ✅ |
| 5679 | `extractor` | ABIERTO → contestaba JSON | **sin respuesta** ✅ |
| 2376 | API de Docker | ABIERTO → pedía certificado | **sin respuesta** ✅ |

  - **Verificado también:** sesión **ssh nueva** entra (el 22 quedó bien) · `https://agoradigital.io` y `https://app.agoradigital.io` → **200** · `https://api.agoradigital.io/health` → **401**, que es la prueba de que **Caddy sigue alcanzando al gateway por el puente de Docker** · `docker logs dl-worker --since 10m` → 115 líneas, **cero** errores de conexión saliente · salida en vivo a los seis servicios que importan: **Redis Cloud `:16705` OK** (tres conexiones establecidas a 18.231.42.38:16705), Supabase, OpenAI, Mistral, Google y MercadoPago, todos `:443` OK · los pollers ciclando (90 escaneos de carpeta en la hora).
  - **VUELTA ATRÁS:** **desasociar el cortafuegos del droplet** desde el panel de DigitalOcean. Un clic, inmediato, sin tocar nada del servidor. Y si algo dejara afuera por ssh, la **consola web del droplet** entra sin pasar por el cortafuegos: esa es la red.
  - **LO QUE ESTO NO ARREGLA — INC-002 sigue exactamente igual.** La llave publicada en el bundle **entra por `api.agoradigital.io:443` → Caddy → `172.20.0.1:3001`**, que es el camino legítimo y sigue abierto porque tiene que estarlo. Cerrar el 3001 en la IP pública **sacó una puerta duplicada, no la puerta.** El cortafuegos no baja ni un punto la prioridad de INC-002: el arreglo sigue siendo que el gateway valide el JWT de Supabase y saque el `organization_id` del token.
  - **De dónde salió:** de tirar del mismo hilo que los hallazgos del agente de Ámono (INC-003, INC-004 y los dos anteriores sin número) — no fue un hallazgo suyo directo, pero fue consecuencia de seguir esa serie. En concreto, del reconocimiento previo a aplicar el cortafuegos, midiendo desde afuera en vez de suponer. La línea de base quedó registrada para poder comparar el después; sin ella, «sin respuesta» no prueba nada.

### Cola de seguridad (al 2026-09-12)

**Cerrados:** ~~**INC-003**~~ — `profiles` escribible por el cliente (2026-09-11) · ~~**INC-004**~~ — `admin_*` sin control de quién llama (2026-09-12) · ~~**INC-005**~~ — puertos de servicio abiertos a internet, cerrados por cortafuegos (2026-09-12) · ~~**Mapas de código**~~ — dejaron de generarse y de publicarse (2026-09-12). Causa raíz: `vite.config.ts` leía `process.env`, y Vite NO expone el `.env` ahí, así que el token estaba puesto y no se usaba — los mapas se publicaban **y** Sentry no los recibía. Arreglado con `loadEnv` + `sourcemap: token ? 'hidden' : false` (detalle en la entrada del 2026-09-12 en Completadas).

Orden vigente (actualizado el 2026-09-12, después de cerrar los mapas de código y la carga web):

1. **Gateway que falla cerrado** — commit `182b616` en la rama `fix/gateway-falla-cerrado`, **commiteado pero sin desplegar**.
2. **INC-002 — gateway con JWT** (ficha arriba). Que el gateway valide el JWT de Supabase y saque el `organization_id` del token. Rotar la llave no sirve.
3. **URLs firmadas en los CUATRO lugares del worker, y recién ahí cerrar los buckets** — `facturas`/`documents` siguen públicos (ver Problemas conocidos). Plan en `PROPUESTA-URLS-FIRMADAS-AGORA.md`. **El quinto lugar, el del navegador, ya está hecho** (2026-09-12: la carga web firma a 24 h). Faltan `integration-poller`, `poller-handoff`, `ftp-sftp-poller` y `zip-processor`; después cerrar los buckets; después la escritura de `facturas`.

> **2 y 3 se planean juntos.** Los dos tocan el frontend y, en buena medida, **los mismos archivos** — `src/lib/pdfJobHelpers.ts` y su llamador `SubirZipPage`: ahí vive tanto la llave del gateway (INC-002) como la subida a `facturas` y el armado de la URL pública (buckets). El JWT necesita que el navegador mande **su sesión** en lugar de la llave; cerrar los buckets necesita que el panel **guarde dentro de la carpeta de su organización y firme la URL**. Se aplican por separado — un despliegue de frontend cada uno — pero **planearlos de una sola vez ahorra trabajo**: es el mismo código tocado dos veces si se piensan sueltos.

### Actualización 2026-09-14 → 2026-09-19 (sesión de chat, verificado contra la base)

**Funciones abiertas a cualquiera — CERRADO 2026-09-14.** Nueve funciones SECURITY DEFINER sin ningún control interno eran ejecutables por `anon`/`authenticated` (por defecto `CREATE FUNCTION` otorga EXECUTE a PUBLIC). Reconocimiento: nombres buscados en todo el repo, en `dist/` (lo desplegado) y en la base.
- Sólo `service_role` (`REVOKE … FROM PUBLIC, anon, authenticated`): `add_credits`, `charge_credit`, `gateway_create_pdf_job`, `gateway_register_rejected_file` (llamador conocido: worker con llave de servidor) y `finalize_pdf_job`, `increment_processed_documents`, `get_job_document_total`, `activate_integration` (no aparecen en ningún lado: restos).
- `get_system_avg_confidence`: cerrada a `PUBLIC`/`anon`, **se deja a `authenticated`** — la usa el tablero del CLIENTE (`useClientJobs.ts:223`, tarjeta «Efectividad del sistema»), no Monitoreo como decía `ONBOARDING-TECNICO-AGORA.md:264`. Si falla, la pantalla no rompe: el número desaparece en silencio.
- `debit_credits` (Ámono) cerrada igual; `amono_saldo`/`amono_vinculo_consumir` las cerró el agente de Ámono.
- Validado por comportamiento: documentos reales de Drive cobraron normal después (14/09 10:03, $0,85).
- Vuelta atrás: `GRANT EXECUTE ON FUNCTION … TO authenticated, anon;`

**TRUNCATE — CERRADO 2026-09-19.** Probado en tabla de juguete: con RLS activa y sólo política de lectura, `anon` NO pudo borrar filas con DELETE pero SÍ vació la tabla con TRUNCATE (RLS no cubre TRUNCATE). Antes de quitarlo: ninguna función contiene TRUNCATE; en `pg_stat_statements` sólo hay uno interno de Supabase y la prueba. Aplicado `REVOKE TRUNCATE ON ALL TABLES IN SCHEMA public FROM anon, authenticated` + `ALTER DEFAULT PRIVILEGES … REVOKE TRUNCATE`. Resultado: 25/29 → 0. Lectura/edición intactas, `service_role` intacto; simulado como usuario de Aignition: ve sus 44 procesos y su saldo. Residuo: las tablas que cree `supabase_admin` siguen naciendo con el permiso (no lo podemos cambiar). Vuelta atrás: `GRANT TRUNCATE ON ALL TABLES IN SCHEMA public TO anon, authenticated;`

**Vigilancia horaria — desde 2026-09-14.** Tarea programada de Cowork (`trig_014A7rnHcReFkVQyfN4Xyqwa`), días hábiles 11–23 UTC: lee `pdf_jobs` de los últimos 70 min y procesos trabados >30 min, y avisa al teléfono del director. Sólo lectura. Línea de base: ~1.496 procesos en 60 días, 8 fallos de procesamiento, 2 rechazos de formato. **Desde 2026-09-19 también vigila pagos** (renombrada «Agora — vigilancia de procesos y pagos»): A) pagos `approved` sin saldo cargado — compra nueva por `credits_accrued`, camino viejo por falta de fila `purchase` con ese `gateway_payment_id`; va siempre primero; B) pagos en `review`; C) compras que cargó la revisión automática en la última hora (el aviso de Mercado Pago no llegó: hay que mirarlo); D) movimiento de compras nuevas, con aviso si se vendió con el dólar de respaldo. Los `pending` no avisan (abrir el pago y no pagar es normal). Corre sólo días hábiles; la revisión automática del servidor corre todos los días.

**Sentry (verificado 2026-09-14).** El proyecto `javascript-react` (frontend) recibe eventos. **No hay proyecto para el worker**: los fallos de procesamiento no le avisan a nadie (la vigilancia horaria es el paliativo). El proyecto `amono` no recibió eventos (Ámono no está en producción). El único problema registrado (`JAVASCRIPT-REACT-3`, pestaña vieja durante un despliegue, un solo usuario) se marcó resuelto. **El arreglo de source maps del 12/09 sigue sin verificar**: no hubo ningún error desde entonces.

**Saldo = dólares.** `organization_credits.balance` es `numeric(12,4)` en USD; `add_credits`, `charge_credit` y `debit_credits` operan en USD sin convertir. `credit_price_tiers` habla de «créditos a 0,30» — es la pantalla de compra por tramos, otra unidad, y se retira (BILLING-COMPRA-5).

## Problemas conocidos

| Problema | Qué pasa | Rodeo |
|---|---|---|
| `facturas` sigue público | Sus archivos se sirven sin autenticación a quien tenga la URL exacta. La política que permitía *listar* el contenido ya se eliminó (2026-09-10) | Ninguno todavía. Quedan **cuatro** lugares que arman URL pública, **todos en el worker** (`worker/integration-poller.mjs:74`, `worker/poller-handoff.mjs:148`, `worker/ftp-sftp-poller.mjs:67`, `worker/zip-processor.mjs:68`); el quinto, el del navegador (`src/lib/pdfJobHelpers.ts`), **ya firma desde el 2026-09-12**. Recién con los cuatro firmados se pueden cerrar los buckets: hasta entonces el OCR descarga por URL pública |
| El gateway rechaza `.rar` que el worker sí sabe abrir | La lista de formatos de `gateway.mjs` rechaza `.rar` (`error_type=rejected`, ej. job `42267b56` 2026-09-10 14:39 «Formato no permitido: .rar (111.rar)»), pero `zip-processor.mjs` extrae RAR con `bsdtar`. El gateway veta algo que el worker podría procesar | Ninguno todavía — inconsistencia anotada, sin tocar. Alinear la lista del gateway con lo que el worker soporta (o decidir explícitamente no soportar RAR y sacarlo del worker) |
| `facturas` — dos políticas de escritura todavía abiertas | Desde el 2026-09-12 el panel **ya no escribe ahí** (va a `documents/{org_id}/uploads/`). Lo que queda son `facturas_authenticated_upload` (INSERT) y `facturas_authenticated_update` (UPDATE), que dejan a **cualquier usuario autenticado** escribir o pisar cualquier archivo del bucket si conoce el nombre, sin filtro por organización | **Borrarlas está pendiente a propósito**: esperan a que el director pruebe un ZIP desde el panel. Son la red por si hay que volver atrás |
| Predicados de dueño sin organización (deuda de INC-003) | Las políticas de dueño preguntan solo «¿existe un perfil con mi id y `role='owner'`?» — si sos dueño de algo, no de QUÉ organización. El revoke de INC-003 cierra la vía de entrada (el cliente ya no puede escribir `role` ni `organization_id`), pero el predicado sigue mal escrito | Ninguno todavía — arreglo aparte: acotar cada predicado a `pr.organization_id = <tabla>.organization_id` |
| Permisos de fábrica demasiado amplios | `anon` y `authenticated` tienen DELETE en 31 tablas, de los dos productos. DELETE está contenido por RLS, así que hoy no es explotable — pero es permiso dado de más. **TRUNCATE ya no: quitado el 2026-09-19** (ver Actualización abajo) | Ninguno todavía — merece una revisión completa de los grants del esquema `public` (aplicando la REGLA de INC-003: primero se quita el permiso de tabla, después se devuelven las columnas) |
| Organización huérfana duplicada desde `AuthContext` | El camino viejo `createOrgPostConfirmation` (`AuthContext.tsx:95-111`) inserta en `organizations` ANTES del perfil. Si `fetchProfile` falla de forma pasajera y quedó `dl_pending_org` en localStorage (lo guarda `LoginPage.tsx:171` cuando falta confirmar el email), crea una organización nueva que queda sin perfil. Desorden, no agujero | Ninguno todavía. El INSERT de perfil que sigue ya falla siempre (clave primaria y, desde INC-003, permisos) |
| Subida desde el panel falla otra vez (2026-09-14) | Dos intentos `frontend_upload` de Aignition a las 09:39 UTC terminaron en `error_type=processing` con «Error subiendo archivo: new row violates row-level security policy». Simulado como el usuario: la política `documents_insert_propia` SÍ deja pasar la ruta `{org_id}/uploads/…`, así que el panel desplegado estaría escribiendo en otra ruta o bucket. Causa sin confirmar | A Menara no le afecta (carga sólo por `supabase_storage`, 1.034 procesos en 45 días). Requiere leer el bundle desplegado y desplegar frontend: zona cerrada |
| Saldo y libro de movimientos no cuadran | `add_credits_admin` y el disparador del regalo de alta (`assign_free_plan_on_org_create`, US$ 5 desde `billing_plans.free.balance_usd`) suman saldo sin escribir fila en `credit_transactions`. Al 2026-09-14: Menara +US$ 3.629,80 sin asiento (cargas manuales del pospago) | Tarjeta BILLING-LIBRO: que escriban su fila. Toca el camino de Menara → OK aparte |
| «Efectividad del sistema» ignora los filtros | La tarjeta muestra el promedio de `confidence_score` de TODAS las organizaciones (1.506 filas), es la autoevaluación del modelo y no cambia con los filtros de cliente/fecha. No mide aciertos reales | Ninguno. Es un tema de rótulo/UX |
| Proveedor cuyo nombre vive sólo en el logo | El OCR devuelve el encabezado como **imagen** (`![img-0.jpeg]`) y el modelo nunca recibe la razón social, así que deja `proveedor` vacío — **que es lo correcto: mejor vacío que inventado**. Visto en URBANA INSTALACIONES (perfil id 6), PARATE LIBRERIA y ahora AUCKLAND TURISMO S.A. (CUIT 30-70912762-0) | Rodeo aplicado el 2026-09-12: el director editó el PDF agregando el nombre y reprocesó — funcionó (salió marcado como duplicado, esperado). **Solución de fondo disponible, NO aplicada**: cargar una ficha en `proveedor_profiles`; el worker matchea por CUIT dentro del texto del OCR y el CUIT de Auckland **sí** viene en el texto. Queda a decisión del director cuándo cargarla |

## Stack

React 18 + TS + Vite + shadcn/ui + Tailwind · Supabase (PostgreSQL + RLS + Realtime) · Worker Node ESM (`worker/*.mjs`) en Docker (VPS DigitalOcean) · BullMQ + Redis Cloud (sa-east-1, DB renombrada `data-land`→`agora`; `maxmemory-policy=noeviction` desde 2026-07-04 para que BullMQ no pierda jobs por eviction), queue `pdf-processing` · MercadoPago IPN · Integraciones: Google Drive ✅, Firebase Storage ✅, Supabase Storage ✅, SFTP ⛔.

## Producción

> **⚠️ MIGRACIÓN DE DOMINIO (2026-06-28, GTM-04):** producción ahora en **agoradigital.io**. App = `app.agoradigital.io`, worker/gateway = `api.agoradigital.io` (directo al :3001, SIN path `/worker`), landing = apex `agoradigital.io`. Callback OAuth = `https://api.agoradigital.io/api/auth/google/callback`. `dataland.aignition.net` y `automation.aignition.net` **DECOMISIONADOS** (bloques Caddy + DNS removidos; n8n dado de baja, GTM-07). El path de archivos del front sigue siendo `/var/www/dataland/` (no cambió). Marca de producto = **Agora**. Detalle completo: `GTM-01-migracion-agoradigital.md`.

- **Frontend**: `https://app.agoradigital.io` (landing en apex `agoradigital.io`) → VPS `root@157.230.231.207:/var/www/dataland/`
- **Worker/gateway**: `v1.9.9` en `root@157.230.231.207:/root/worker/` (Docker Compose). Commits worker recientes: f4987cb (TASK-96) + TASK-110 + TASK-114; **7f568fe** = TASK-108 (gateo de adjuntos por org) + DEC-019/REMOVE-DEDUP (pollers sin dedup) DEPLOYADO 2026-06-24.
- **Supabase**: `klhbgsiatzbmxbkzpbzv`
- **Superadmins** (DB, `is_superadmin=true`): `sbrasesco@outlook.es`, `javierginez@gmail.com`. ⚠️ `arcademy.dev@gmail.com` NO figura como superadmin en DB.

## Deploy workflow

```bash
# Frontend — siempre SCP, nunca Netlify ni git pull
npm run build
rm -rf /var/www/dataland/assets
scp -r dist/. root@157.230.231.207:/var/www/dataland/
# Worker — siempre docker compose, nunca docker run manual
scp worker/*.mjs root@157.230.231.207:/root/worker/
ssh root@157.230.231.207 "cd /root/worker && docker compose build && docker compose up -d --force-recreate"
# Verificar: docker logs dl-worker --tail 30 ; curl -s http://localhost:3001/health
```

- **`STORAGE_BUCKET`** (DEC-022): el worker lo lee de `/root/worker/.env` del servidor (no va al repo; `worker/.env.example` todavía dice `facturas`). Hoy vale **`documents`** (desde 2026-09-10). Alcanza al camino del ZIP manual (`zip-processor.mjs`: subida de los PDFs extraídos + URL pública que va al OCR) y, indirectamente, a cualquier código nuevo que lea la variable. ⚠️ El default en código sigue siendo `'facturas'` (`zip-processor.mjs:24`): si la variable falta del `.env`, el worker vuelve a escribir en el bucket congelado sin avisar. Los pollers (Drive, handoff, SFTP) tienen `documents` fijo en el código y no la leen.

## Pipeline (estado real)

```
Frontend / Integration poller → POST /api/enqueue → gateway.mjs (:3001)
  → BullMQ "pdf-processing" (Redis) → worker.mjs → document-processor.mjs
  → Supabase (resultados) + Drive/Firebase/Supabase (outputs)
```

- **gateway.mjs rutas**: `POST /api/enqueue` (Bearer) · `/api/mp/create-preference` + `create-custom-preference` (Bearer) · `/api/mp/webhook` (sin auth, IPN) · `/api/deposit-row` (Bearer) · `/api/drive/folders` + `set-folder` (Bearer) · `/api/auth/google/callback` (sin auth) · `/api/integrations/init-folders` + `test-connection` + `migrate-folders` (Bearer) · `/api/metrics` (Bearer, proxy :9090) · `/api/prompt` (Bearer, prompt del extractor read-only, TASK-114) · `/health`.
- **metrics.mjs (:9090)**: queue_depth (waiting/active/delayed), latency_ms (p50/p95/avg), error_rate_pct, totals. Consumido por MonitoringPage vía `/api/metrics`.
- **Env worker** (`/root/worker/.env`): `REDIS_HOST/PORT/PASSWORD`, `SUPABASE_URL/SERVICE_KEY`, `WORKER_CONCURRENCY=3`, `WORKER_VERSION`, `METRICS_PORT=9090`, `GATEWAY_PORT=3001`, `GATEWAY_API_KEY`, `MP_ACCESS_TOKEN`, `GATEWAY_URL=https://api.agoradigital.io`, `STORAGE_BUCKET=documents` (DEC-022), `GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI`, `FRONTEND_URL`, `NODE_ENV=production`. **Frontend**: `VITE_WORKER_API_KEY=staging-key-2026`, `VITE_WORKER_GATEWAY_URL` (base sin path).

## Frontend (misc)

- Rutas: layout con `<Outlet />` en `App.tsx` (AppShell se monta una sola vez); `SuperadminRoute` para MonitoringPage. Health header `Authorization: Bearer staging-key-2026`.
- Tipografía: **Inter** (UI) + **Lora** (`font-lora`, números en tarjetas métricas). Google Fonts en `index.html`.

---

## Billing / créditos

- `organization_credits.balance` numeric(12,4) USD. `plan_id` solo logging/trazabilidad.
- `charge_credit(...)` SECURITY DEFINER: descuenta `(base + features + polling) × docs`. Base = `billing_plans.price_per_doc` del plan `basico` (COALESCE $0.30 si NULL/ausente). Editable desde MonitoringPage→Precios sin redeploy. (Reemplazó el hardcode de TASK-90.)
- `feature_pricing_multipliers` (cost_usd; RLS read `authenticated_read_feature_pricing`). `get_price_breakdown()` SECURITY DEFINER → jsonb `{base_price, features, polling, total_per_doc}` (lee billing_plans + tenant_integrations + features + polling_tiers; `master_file` solo drive+xlsx **con output_enabled=true**).
- `trg_assign_free_plan` — plan "free" + US$ 5 de saldo a org nueva (sale de `billing_plans.free.balance_usd`; corregido el 2026-09-19, decía $20).
- `payments`: `gateway_preference_id` (al crear), `gateway_payment_id` (al IPN), `status` default `pending`.
- **Paquetes con bono y monto libre:** en rediseño según `ESPEC-COMPRA-DE-SALDO.md` (tablero, BILLING-COMPRA-1 a 6).

### feature_pricing_multipliers (prod — ⚠️ precios DINÁMICOS, no fijos)

| key | label | cost_usd | active |
|---|---|---|---|
| integration_drive | Google Drive | $0.20 | true |
| integration_firebase | Firebase Storage | $0.15 | true |
| integration_supabase | Supabase Storage | $0.15 | true |
| integration_sftp / ftp | SFTP / FTP | $0.03 | true |
| master_file | Excel acumulativo | $0.05 | true |
| xlsx_output | Formato Excel (.xlsx) | $0.00 | false |
| human_review | Revisión humana | $0.00 | true |
| polling_interval_1min | Escucha 1 min | $0.00 | true |

Editable desde MonitoringPage→Precios (superadmin) vía RPC `update_feature_cost`. Los montos "validados" en el historial son la config de ese momento, no valores hardcodeados.

### document_types (config global — NO hardcodear)
Tabla `document_types` (code, label, sort_order, active), RLS read `authenticated_read_document_types`. `code` = valor canónico de `pdf_job_rows.tipo_documento` que produce la IA: `FACTURA_A/B/C/M`, `NOTA_DEBITO_A/B/C`, `NOTA_CREDITO_A/B/C`, `ORDEN_COMPRA`, `SOLICITUD_COTIZACION` (fuente: prompt en `document-processor.mjs`). El dropdown de edición manual (`EditRowModal` vía hook `useDocumentTypes`) muestra `label` y guarda `code`. Alta de tipo = INSERT, sin redeploy. Global por ahora (regionalización por país = futuro). Panel admin superadmin en MonitoringPage→Tipos de doc (TASK-111, RPCs `upsert_document_type`/`toggle_document_type`; ✅ en prod). **`codigo_afip` (AFIP-CODE-CONSISTENCY, 2026-06-30, ✅ Validated en prod):** `document_types` ganó la columna `codigo_afip` (2 dígitos: 01,02,03,06,07,08,11,12,13,51; OC/cotización=NULL). `pdf_job_rows.codigo_afip` ya **NO** lo extrae la IA: lo **deriva el worker** (`document-processor.mjs:getAfipCodeMap`, cacheado 10 min) desde `tipo_documento` contra `document_types` → tipo y código nunca se contradicen (problema original del cliente: ERP no contabilizaba). El prompt devuelve `codigo_afip:null`. **Detección de TIPO en 3 niveles** (porque OCR a veces no captura la letra/recuadro): (1) LETRA del encabezado; (2) "Cod. NN" impreso → tabla; (3) inferencia fiscal (emisor Monotributo→C; RI+IVA discriminado/receptor RI→A; RI+IVA no discriminado/receptor CF→B); nunca clase sin variante (si no, null). Apertura del prompt = "analista que interpreta", no "extractor". Editable sin redeploy desde la tabla. **AFIP-CATALOG (2026-07-04):** `document_types` ganó columna `country` (default `AR`, dimensión de regionalización DEC-018) y se cargó el **catálogo AFIP completo (97 códigos)**: los 12 que el sistema procesa quedan `active=true`; los otros 85 (recibos, liquidaciones, tiques, FCE MiPyMEs, granos, remitos) entran `active=false` = catálogo de referencia. Los menús (`useDocumentTypes` filtra `active=true`) y la IA (lista en el prompt) NO se ven afectados; `getAfipCodeMap` en el worker ahora lee `&active=eq.true` (derivación/guardrail solo sobre tipos activos). Activar un tipo nuevo = poner `active=true`. Labels del catálogo sin acentos (referencia). Otro país = filas con `country` distinto (unique key sigue en `code`; migrar a `(country,code)` cuando entre un 2º país, TASK-113).

## RPCs relevantes

| RPC | Tipo | Propósito |
|---|---|---|
| `charge_credit(p_org_id, p_amount_usd, ...)` | SEC DEFINER | Descontar saldo por doc procesado |
| `add_credits(p_organization_id, ...)` | SEC DEFINER | Agregar saldo + `credit_transactions`. Desde gateway/service key |
| `add_credits_admin(p_org_id, p_amount_usd)` | requiere superadmin `auth.uid()` | Agregar saldo desde UI (NO service key) |
| `approve_document_row(p_row_id bigint)` | — | Aprueba doc + `corrected_documents`; si todos ok → job `done` |
| `get_all_tenants_admin()` / `get_tenant_jobs_admin(p_org_id)` | SEC DEFINER | Panel superadmin (bypass RLS) |
| `gateway_create_pdf_job(...)` | SEC DEFINER | Crea `pdf_jobs` desde integraciones |
| `get_price_breakdown()` | SEC DEFINER | Desglose de costo por doc |
| `gateway_register_rejected_file(...)` | SEC DEFINER | Crea `pdf_jobs` error_type='rejected' para archivo rechazado (TASK-110) |
| `upsert_document_type(p_code, p_label, p_sort_order?, p_active?)` | SEC DEFINER + guard superadmin | Alta/edición de `document_types`. `code` normalizado a mayúsculas e inmutable (clave de conflicto); `sort_order` default max+10 (TASK-111) |
| `toggle_document_type(p_code, p_active)` | SEC DEFINER + guard superadmin | Activa/desactiva un tipo de documento (TASK-111) |
| `get_tenant_attachment_flags()` / `set_tenant_attachment_extraction(p_org_id, p_value)` | SEC DEFINER + guard superadmin | Leer/setear el flag `extract_embedded_attachments` por tenant (upsert en `tenant_feature_flags`). Toggle de TASK-108 en MonitoringPage→Tenants |

---

## Tareas

### 🟡 Backlog

| Task | Descripción | Prioridad |
|---|---|---|
| **TASK-102** | AI-REFINE-PROMPT: ajustar prompt para facturas de servicios | 🟡 Media |
| **VISION-TYPE-DETECTION (🔄 EN PROGRESO 2026-07-09, TASK-139)** | Pasarle la IMAGEN a GPT-4.1-mini (multimodal) para leer la LETRA A/B/C/M que el OCR de texto (Mistral) pierde cuando está dibujada en un recuadro. ⚠️ **RENDER = PyMuPDF (fitz), NUNCA pdftoppm**: en el contenedor Alpine del worker, pdftoppm sin fuentes (ej. Courier New Bold) deja el PNG **en blanco** → los modelos ven un form vacío → 0% acierto; PyMuPDF (ya está en el worker) sustituye fuentes → 100%. Probado 9/9 con gpt-4.1-mini/5-mini/5-nano (script read-only `worker/scripts/vision-type-test.mjs`, commit 08bfc84). Falta tanda B/C/M/monotributo para elegir modelo (nano vs mini; costo ~$0.36–1.33/1000 docs, irrelevante vs $300 que factura). Es el fix de fondo de AFIP-CODE-CONSISTENCY. **DIFERIDA**: la prueba está hecha (9/9) pero la implementación en prod NO se construyó — el worker sigue con solo texto OCR; los casos del cliente ya los resuelven la detección de 3 niveles + guardrail + derivación codigo_afip. Reabrir solo si aparecen fallas nuevas de lectura de letra. **ARQUITECTURA B (visión aislada) — en prod Aignition, pendiente validación 2026-07-10, TASK-139:** Se probó primero la híbrida (A: imagen en la misma llamada de extracción) y en el test ON/OFF sobre prod **los importes se movieron** (RAPIFERR: neto 346k→271k) + un `numero_comprobante` levantó `ORIGINAL` del sello → se **descartó A** y se reescribió a **B**. En B la extracción de texto (`extractWithOpenAI`) queda **byte-idéntica a HEAD** (importes/CUIT salen de ahí, sin imagen → no se pueden correr); una **llamada de visión AISLADA** (`detectTipoWithVision`, `OPENAI_VISION_MODEL` def gpt-4.1-mini, render PyMuPDF via `renderFirstPageDataUrl`) mira la 1a página y, si devuelve un tipo válido (`VALID_TIPOS`), **sobrescribe SOLO `tipo_documento`** antes del guardrail `correctTipoByPrintedCode`+derivación codigo_afip. Gateado por `tenant_feature_flags.vision_type_detection` (Aignition ON, resto OFF); OFF o cualquier fallo de render/visión → no toca nada (fail-safe, nunca throw). `worker.mjs`: `getVisionFlag` + `vision_enabled` en ambos payloads (sirve igual para B). Dockerfile ya trae python3+pymupdf. Validado local: node --check + balance + render smoke-test. **Pendiente**: deploy worker + validación ON/OFF (importes deben quedar idénticos ahora; el tipo solo debe mejorar) + tanda variada A/B/C para ver el beneficio. Rollback = flag Aignition→false. Cambios SIN commitear (deploy-only hasta validar). | 🟠 Alta |
| **TASK-103** | RESEARCH-AFIP: investigar integración validación AFIP | 🟢 Baja |
| **TASK-113** | ÉPICO REGIONALIZACIÓN multi-país (escalado futuro, DEC-018): país como dimensión (prompt/document_types/campos por país). Solo AR hoy | 🟢 Baja |
| **TASK-66** | Landing visual refinement — dejar para el final | — |

### ✅ Completadas (en prod)

**Sesión 2026-09-12 (CARGA-WEB-A-DOCUMENTS — la subida desde el panel estaba ROTA en producción: «new row violates row-level security policy»)**: tres intentos fallidos (14:28, 14:57, 15:08), **todos de Aignition**; ningún cliente afectado, porque esa vía no se usaba desde el 13 de agosto. · **CAUSA, sin maquillaje** — al cerrar el agujero de enumeración el 2026-09-10 se borró la política `facturas_public_select`. Esa misma política era **lo único que le daba LECTURA sobre `facturas` a un usuario con sesión**, y `.upload(..., {upsert:true})` **lee antes de escribir**. Se cerró la puerta correcta sin ver que el panel la usaba para trabajar. No se detectó durante **dos días** porque esa vía no tenía tráfico. ⚠️ **REGLA QUE QUEDA: antes de borrar una política, listar qué caminos del producto dependen de ella. «No tiene tráfico» no es «no se usa»; es «todavía no se notó».** (También anotada en ZONA CERRADA → Seguridad / DB.) · **ARREGLO — no se restauró la política vieja**, se hizo como tiene que quedar, que además es el punto 3 de la cola para el camino del navegador. **Base**: dos políticas nuevas sobre `documents`, acotadas por organización — `documents_insert_propia` (INSERT) y `documents_select_propia` (SELECT), ambas `bucket_id='documents' AND (storage.foldername(name))[1] = current_org_id()`. Hacen falta las dos: firmar una URL requiere SELECT. **Frontend** (`src/lib/pdfJobHelpers.ts:uploadFileToWorker`): sube a `documents/{org_id}/uploads/{jobId}.{ext}` con **`upsert:false`** (el `jobId` es un UUID nuevo en cada envío — `createPdfJob` crea un job por intento —, así que no hay colisión posible y **escribir ya no requiere ningún permiso de lectura**), **exige `organizationId`** y falla claro si falta (sin él la ruta quedaría `null/uploads/…` y la política la rechazaría con un error ilegible), y manda al gateway una **URL FIRMADA a 24 h** en vez de la pública eterna. · **Compatibilidad verificada ANTES de escribir**: `gateway.mjs:830` compara `new URL(file_url).host` contra el de `SUPABASE_URL` y la firmada tiene el MISMO host (solo cambia la ruta a `/object/sign/` y agrega `?token=`; `new URL()` ignora el query); `gateway.mjs:825` exige `https://`, que cumple; y **nada en el worker corta la URL en el «?» ni deduce el tipo por su extensión** (`worker.mjs:314` y `zip-processor.mjs:307` hacen `fetch(file_url)` directo, `zipPath` es un nombre fijo `input.archive`, el `file_type` lo manda el frontend desde `file.name`, y el único `split('?')` del worker está en la ruta del webhook de MercadoPago). Comprobado además firmando una URL real contra producción: host idéntico, HTTP 200, `application/pdf`, 747 KB, `%PDF-1.7`. · **Validado en producción**: `documents/6b505051-…/uploads/e0b1fc76-….pdf` a las 15:27:43 (y una segunda a las 15:36:41); los dos jobs `frontend_upload` cerraron `done_with_warnings` y `done`, 2 documentos cada uno, 0 fallidos; **cero escrituras a `facturas`**. Bundle `main-iiPPxedn.js`, respaldo `/var/www/dataland.bak-20260912-172325`. · ⚠️ **DEPENDENCIA que vale anotar: este diseño sólo es seguro porque INC-003 le quitó al cliente la escritura de `profiles.organization_id`.** `current_org_id()` lee esa columna; sin aquel arreglo, cualquiera se cambiaba la organización y esto se construiría sobre arena. · **PENDIENTE (a propósito)**: borrar `facturas_authenticated_upload` y `facturas_authenticated_update`. Esperan a que el director pruebe un **ZIP** desde el panel — son la red por si hay que volver atrás.

**Sesión 2026-09-12 (AVISO-CON-MOTIVO-REAL + SOURCEMAPS-FUERA-DE-LA-VÍA-PÚBLICA — reporte del director: «el cartel nombra causas que no son las reales»)**: dos arreglos de frontend en un mismo despliegue (bundle **main-BdgVpYTm.js**). · **AVISO-CON-MOTIVO-REAL** — `JobDetailHeader.tsx` mostraba un texto FIJO: «…requieren tu revisión (por ejemplo, importes que no cierran o facturas con descuento)». Ese «por ejemplo» **inventaba una causa**: en el job `1c119832` el motivo real era `CAMPOS_FALTANTES` (faltaba el proveedor) y el cartel mandó a desconfiar de importes que estaban bien — media hora perdida. Ahora el header recibe `rows` desde `ProcesoDetailPage` y lista, por cada documento `doc_status='warning'`, `warningReasonLabel(warning_reason)` + `source_file`. **Sin consulta nueva**: `usePdfJobRows` ya hacía `select('*')`, el dato ya viajaba; solo faltaba pasarlo. ⚠️ **REGLA (queda): el texto de respaldo NO nombra causas de ejemplo.** Si no sabemos el motivo, decimos que hay advertencias y mandamos a mirar — en un producto contable, un aviso que apunta a la causa equivocada es peor que uno vago. Hoy el respaldo casi no se usa: el trigger siempre setea una razón (fallback `REVISION`), verificado 9/9 filas `warning` con motivo. Validado corriendo la `warningReasonLabel` real contra las filas reales: job `1c119832` → «Faltan datos clave — 509623-auckland 9-2.pdf»; job `0e26781b` → «El total no cierra con neto + IVA — SIDERSA 977569.pdf». · **SOURCEMAPS** — `vite.config.ts` generaba los mapas SIEMPRE (`sourcemap: true`) pero el plugin de Sentry —único que los sube y después los borra de `dist/`— se activaba con `process.env.SENTRY_AUTH_TOKEN`. **Causa raíz: Vite NO expone el `.env` en `process.env` dentro del archivo de configuración**, así que con el token bien puesto en `.env` el build tomaba siempre la rama sin token: los `.map` quedaban publicados en producción **y** Sentry nunca los recibía. Se perdía por los dos lados, en silencio. Arreglo: `loadEnv(mode, process.cwd(), '')` + `const token = process.env.X || env.X` (sirven los dos caminos: CI/servidor y `.env` local) + `sourcemap: token ? 'hidden' : false` (`'hidden'` genera el mapa para Sentry sin dejar el comentario `//# sourceMappingURL` en el JS; sin token no se generan, porque no habría quien los suba ni quien los borre) + aviso en consola si se compila producción sin token. ⚠️ **REGLA (queda): en `vite.config.ts` las variables se leen con `loadEnv`, nunca con `process.env` a secas** — si no, el `.env` está y no se usa. Verificado por comportamiento: `npm run build` **a secas** → «Successfully uploaded source maps to Sentry» + `find dist -name "*.map"` vacío; con el token desactivado → aviso en consola + 0 mapas generados. En prod, `SubirZipPage-B63UJkcz.js.map` ya no se sirve (devuelve el `index.html` del fallback de SPA, idéntico byte a byte a una ruta inventada — **no es un 404 literal porque Caddy tiene catch-all de SPA**, pero el archivo no está). ⚠️ Lo ya publicado no se despublica: el código fuente que estuvo disponible, estuvo. · **Deploy** — respaldo previo en `/var/www/dataland.bak-20260912-164517` (ver `RUNBOOK-ROLLBACK.md`, que ahora SÍ cubre frontend). **Validación visual de los dos carteles: PENDIENTE del director** (requiere sesión iniciada; verificado en su lugar sobre el bundle desplegado: 0 ocurrencias del texto viejo, lógica y etiquetas presentes).

**Sesión 2026-08-27 (COMPROBANTE-REF-LABELS-2 — reporte Menara: 3 notas de crédito de Menara Corralón con el número de la orden de compra)**: variante nueva de la familia COMPROBANTE-GUARD. **Menara Corralón SA** (CUIT 30-71778445-2, empresa hermana del tenant — persona jurídica DISTINTA de Menara Construcciones 30-50382642-5; el motor las distingue bien por CUIT) imprime el número real con guión («ORIGINAL 0109-00016855») y la orden de compra como «**O. CPRA :** A010900029003/» — **letra+12 dígitos pegados = el formato de MÁXIMA confianza del lector** (patrón Elsener) → le ganaba al real. La exclusión de etiquetas del 21/08 cubría «Orden de Compra»/«O.C.» pero NO la abreviatura «O. CPRA» (verificado contra el OCR real de la fila 4747: el fix anterior no cubría este caso). Las 3 filas originales (4745-4747, 10/08, pre-GUARD) salieron **Exitosas sin ninguna marca** — importes cierran, número verosímil: la única red posible es leer bien el papel. Fix aprobado (opciones 2+3): (1) **REF_LABEL ampliado** en `comprobanteFromOcr` con `o.cpra` / `ord.compra` / `o/c` / `fac.rel` (la misma factura imprime «Fac.Rel.:» al pie — veneno en potencia desactivado). Diff +4/−1; **tests 15/15** (OCR real de Corralón → 0109-00016855 + 4 variantes excluidas + no-regresión completa Lelli/Culzoni/Elsener/Navone/URBANA). (2) **Perfil de proveedor Corralón** (id 7, vía RPC con sesión simulada): layout + «O.CPRA nunca es el comprobante» + relación societaria documentada — el perfil solo NO alcanzaba (el lector determinístico no lee perfiles y le gana a la IA), es complemento. **Validado E2E** (re-subida por Drive de Aignition): los 3 salieron con los números REALES (16855/16854/17351), NOTA_CREDITO_A, PV 0109, gap 0, emisor/receptor correctos, sin duplicado (el número corregido difiere del guardado → entran como docs nuevos con CSV). **Decisión del director: las 3 filas viejas de Menara quedan como histórico** (no se corrigen por atrás); el tenant re-sube en su tenant cuando quiera.

**Sesión 2026-08-21 (DOCS-SEARCH-EXPORT-SERVER-SIDE — reporte de Fernando/Menara: «los filtros parecen funcionar solo sobre la página»)**: diagnóstico: los filtros de fecha/cliente de Documentos YA eran server-side (filtran todo el historial y paginan el resultado); lo page-scoped eran el **buscador de texto** (filtraba solo los 50 visibles) y el **export Excel/CSV** (exportaba solo la página visible) — de ahí la percepción del tenant. Además el filtro por cliente armaba un pre-query de jobs + `.in(job_id,[todos los jobs del cliente])` = la misma bomba de URL de MisProcesos, latente con clientes grandes. Fix (solo frontend: `useAllDocuments.ts` + `DocumentsPage.tsx`): (1) **buscador al query** — `searchText` viaja en los filtros con debounce 400ms y `.or` ilike sobre proveedor/receptor_nombre/numero_comprobante/cuit/receptor_cuit, sanitizado (coma/paréntesis/% neutralizados, mínimo 2 chars), reset a página 1; el contador «Mostrando X de N» refleja la búsqueda. (2) **export de TODO lo filtrado** — `fetchAllDocumentsForExport(filters)` trae hasta 5000 facturas que matchean + sus OCs en tandas de 500 row_ids (bigint cortos → URL siempre chica), botones con estado «Exportando…», aviso si trunca. (3) **filtro cliente vía embed** — `.eq('pdf_jobs.client_id', …)` sobre el `pdf_jobs!inner` ya presente; desaparecen el pre-query y el `.in()` que crecía con el histórico. Refactor interno: `fetchDocsWindow(filters, from, to)` compartido entre página y export (la ventana es sobre FACTURAS; las OCs cuelgan de su factura). Verificado: esbuild 2/2, sanitize 7/7, tsc 4 preexistentes/0 nuevos, chunk DocumentsPage 25.75→26.26 KB (bundle inicial intacto), build main-POGKC2yf. Validado en pantalla por el director (búsqueda cross-página + export completo del rango). Recordatorio semántico: el filtro de fecha de Documentos filtra por FECHA DE LA FACTURA (dato impreso), no por fecha de procesamiento.

**Sesión 2026-08-10/11 (MISPROCESOS-PAGINATION — reporte Menara «Error desconocido al cargar procesos»)**: Mis Procesos era la única vista de volumen SIN paginar: `usePdfJobs` traía TODOS los `pdf_jobs` de la org y pedía los conteos con `.in(job_id, [TODOS los ids])` → con los ~650 jobs de Menara la URL (~25 KB) superaba el límite del proxy (~16 KB, cruzado el 30-31/07) → el request moría antes de llegar a la DB y el catch genérico mostraba «Error desconocido» (los errores de PostgREST no son instanceof Error). Fix (patrón PERF-DASHBOARD-SCALE, tercera vez): paginación server-side **15/pág** (`.range()` + `count:exact`), filtro de cliente al query (`.eq(client_id)`, reset a pág. 1), conteos `.in()` SOLO con los ids de la página, **canales Realtime con los MISMOS nombres** (zona cerrada) pero refetch debounced 350ms de la página visible, polling de activos sobre la página, y el error muestra el message real de PostgREST. `MisProcesosPage` ganó controles Anterior/Siguiente y perdió el filter client-side (con eso murió 1 de los 5 errores preexistentes de tsc → **baseline 4**). Validado: Aignition paginado OK + Menara volvió a operar la pantalla con ~650 jobs. **REGLA: toda vista nueva de volumen nace paginada server-side; ningún `.in()` puede crecer con el histórico.** ⚠️ Primo latente detectado (2026-08-20): el filtro por cliente de Documentos (`useAllDocuments`) arma `.in(job_id, [todos los jobs del cliente])` — misma bomba de URL con clientes grandes; y el buscador de texto de Documentos filtra client-side (solo la página visible, ídem export). Pendiente de tarjeta.

**Sesión 2026-08-19 (COMPROBANTE-GUARD — reporte Menara: «frenar para editar antes del CSV»)**: dos casos reales de docs `ok` con datos malos que pasaban al CSV: (a) **Culzoni 0020-00006684** — el sistema del proveedor imprime el total con +$0,01 (neto 1.825.590,06 + IVA 383.373,91 = …,97 ≠ …,98); la tolerancia GREATEST(0.5, 1%) lo dejaba pasar → el ERP del cliente no reconcilia. (b) **Lelli 0005-00019083** — la factura imprime «Pedido#: X 0001-00000000-1» y `comprobanteFromOcr` lo matcheaba como comprobante (PV 0001, número 00000000); el número real «Nro: 0005 00019083» va con ESPACIO y el regex no lo veía. **Clave arquitectónica: el «freno» ya existía** — el depósito automático solo incluye `doc_status=ok` (output-depositor filtra `doc_status=eq.ok`) y editar→aprobar regenera el CSV; lo que faltaba era que estos casos CAYERAN en warning. Fix en 3 piezas: (1) **`comprobanteFromOcr` robusto** (document-processor.mjs): itera TODOS los candidatos y descarta los precedidos en su línea por etiquetas de REFERENCIA (Pedido/Remito/O.C./Orden de Compra/relacionad/NP/RE/NC/ND) y correlativos todo-ceros; patrón nuevo «Nro: PV␣correlativo» (espacio, sin guión). Tests 11/11 con los OCR reales + no-regresión (Elsener/Navone/URBANA/clásico). (2) **Warning `COMPROBANTE_DUDOSO`** (trigger classify): número o PV todo-ceros → advertencia (frenado y editable). (3) **Tolerancia aritmética CERO real**: gap > $0,005 → `IMPORTE_NO_CIERRA` (antes GREATEST(0.5, 1%)); impacto medido ~2,4% más advertencias (918 ok de Menara: 896 exactos / 14 centavos / 8 mayores) — ACEPTADO por el director: mejor frenar que CSV que no reconcilia. Migración `comprobante_dudoso_and_zero_tolerance_in_classify`; harness **23/23** (helper `test_clt_eval` ganó `p_iva`/`p_pv` vía DROP+CREATE — lección 42725; `sql/tests/` actualizado); razones verificadas con inserts revertidos. Front: label `COMPROBANTE_DUDOSO` en `warningReasonLabel`. **Validado E2E en prod** (Drive de Aignition): Lelli → ok PV 0005/00019083 importes idénticos; Culzoni → warning IMPORTE_NO_CIERRA con importes fieles al papel. ⚠️ **DECISIÓN del director (2026-08-19): NO habilitar «Editar» en docs Exitosos** — la edición queda SOLO para docs frenados (advertencia/fallido); los `ok` con problema se atienden a mano cuando el cliente reporta. No re-proponer sin nueva discusión. Nota operativa: la tolerancia cero rige desde la migración para TODOS los tenants; los docs de proveedores con sistemas que redondean mal (Culzoni) van a caer recurrentemente en advertencia hasta que el proveedor lo arregle — comportamiento deseado.

**Sesión 2026-08-06/07 (incidente Menara sin saldo → POLLER-BALANCE-GUARD, K2-11)**: **Incidente (2026-08-06)**: Menara sin saldo ($0,60 < mínimo $1 del gateway) → los pollers movían los PDFs a `en_proceso/` ANTES de encolar y el gateway respondía 402 SIN crear `pdf_jobs` → silencio total para el tenant y archivos varados (el poller no relee `en_proceso/`); agravante UX: el rojo del sidebar saltaba recién con saldo ≤0, con $0,60 todo se veía verde. Mitigación inmediata: recarga + mover los PDFs a la raíz a mano. **Fix (✅ Hecho + Validated en prod 2026-08-07; sin migraciones, gateway/worker.mjs/file-mover intactos)**: (1) `poller-handoff.mjs` — `MIN_PROCESSING_BALANCE=1` + `orgHasProcessingCredits` (REST a `organization_credits`, fail-open como el gateway) + check en `runIntegrationPoller` ANTES de `pollFn` (cubre supabase+firebase sin tocar sus archivos; Drive con el mismo helper en su loop): sin saldo NO se toca ningún archivo (quedan en la raíz), log `poller.skip_no_credits` 1×/ciclo y last_polled se actualiza igual; `uploadAndEnqueue` tipa el 402 → `err.code='INSUFFICIENT_CREDITS'`. (2) Devolución ante 402 (carrera de saldo justo): supabase (`moveFile` inverso), firebase (copy+delete inverso), drive (`files.update` inverso; `let enProcesoId` visible en el catch; flag `outOfCredits` corta las subcarpetas) → el archivo VUELVE a la raíz y se recupera solo al recargar; completa el contrato DEC-019 con el camino inverso. (3) `AppShell.tsx` — rojo con `balance < 1` (antes ≤0), texto «Saldo insuficiente para procesar» + saldo real visible; el verde muestra `≈N docs` estimado vía `get_price_breakdown` (best-effort, si el RPC falla no se muestra). Tests inline 7/7 (saldo bajo→skip; umbral exacto $1→pasa, igual que el gateway; sin fila→skip; error del check→fail-open; 402→tipado; 500→sin code). **Validado en prod (Aignition a $0,50, restaurado a $47,50)**: con saldo bajo el PDF quedó en la raíz, 0 jobs, 0 uploads a Aurora, `skip_no_credits` 1×/ciclo en logs, last_polled avanzando, rojo nuevo visto por el director; al restaurar, el ciclo siguiente levantó SOLO el PDF varado → WILTEL FACTURA_A 0028-00152868, `ok`, gap aritmético 0, cobro $0,85 correcto, renombrado por dato en `procesados/` (FILE-RENAME-BY-DATA) — recuperación sin intervención. Pieza 2 (devolución) queda en observación hasta el próximo 402 real (ítem en SEGUIMIENTO K2-9). ⚠️ El umbral $1 es fijo y está ALINEADO con el 402 del gateway a propósito: cambiar el criterio (p.ej. costo real por tenant) = tarea aparte que toca AMBOS lados a la vez, nunca uno solo.

**Sesión 2026-07-25/26 (Monitoreo global + UX + flywheel IA)**: · **MONITOREO-GLOBAL** — Monitoreo ahora ve TODO el sistema (antes las queries directas a `pdf_jobs` pasaban por RLS y mostraban solo la org del superadmin): RPC **`get_monitoring_overview()`** (jobs por estado, errores recientes con org, docs totales/24h, últimos 30 jobs) + RPC **`get_admin_jobs(p_org_id,p_desde,p_hasta,p_status,p_limit,p_offset)`** (lista global con filtros tenant/fecha/estado + paginación; jsonb {total,rows}; DROP+CREATE al agregar p_status para no dejar overload ambiguo — lección 42725). Modal Jobs: tarjetas resumen + tabla multi-org + filtros (Select shadcn + DatePicker) + paginación 15 + **Exportar Excel** (hasta 5000 filas, import dinámico xlsx). · **FIX `get_all_users_admin`** — 42702 `is_superadmin` ambiguo (familia TASK-95, guard sin calificar): la tarjeta Usuarios daba 'error al cargar'; migración `fix_get_all_users_admin_ambiguous`. Usuarios = cuentas de login (7); Tenants = organizaciones. · **UX-MONITOREO-HUBS** — tablero simplificado a **6 tarjetas** (Documentos/Tenants/Usuarios/Worker/Precios/IA): tarjeta **IA** agrupa prompt + correcciones + perfiles (modal-menú); tarjeta **Documentos** agrupa procesos/docs/errores/trabados/cola/tipos como **gráfico de barras horizontales clickeables** (apaisado 2 columnas) y **hereda el semáforo** (rojo si trabados o err>5%, ámbar si cola>10, con ⚠ en el sub); botón **«← Volver»** en todos los modales anidados (X/ESC cierra todo); modales apaisados (Tipos de doc max-w-5xl, editor de perfil 2 columnas). · **VISOR CUADERNO + GESTIÓN PERFILES** — RPC `get_corrections_stats()` (superadmin; totales, top campos, top proveedores con flag tiene_perfil, últimas 15 con detalle IA→humano) + en el MISMO modal la gestión self-service de `proveedor_profiles` (lista, Crear/Editar prellenado, editor con hints/notas/activo → `upsert_proveedor_profile`, sin deploy, caché worker ~10 min) + textos explicativos. · **UX-DATEPICKER-SHADCN** (commit 6f3eae9) — deps nuevas `react-day-picker`+`date-fns`+`@radix-ui/react-popover`; componentes `popover.tsx`/`calendar.tsx`/`date-picker.tsx` (locale es, dd/MM/yyyy, Limpiar, contrato yyyy-MM-dd idéntico a input date); migrados los **6 campos** (Dashboard×2, Documentos×2, Monitoreo/Jobs×2); chunk **lazy** (116KB/33KB gzip, no toca el bundle inicial). ⚠️ **Deploys con cambios de package.json requieren `npm install` antes del build.** ⚠️ npm reporta 13 vulnerabilidades PREEXISTENTES (housekeeping pendiente). · ⚠️ **KANBAN PARTE 2**: el Kanban original llegó al límite de bloques del plan gratis de Notion; las tareas nuevas van a **«📋 Kanban Parte 2 — Roadmap de Escalado»** (misma página madre, IDs `K2-…`); revisar AMBOS tableros. Épico **AI-IMPROVEMENT** ahí: `extraction_corrections` (trigger captura por `auth.uid()`) + `proveedor_profiles` (5 seed, validado) + visor/gestión hechos; few-shot/routing-métricas/fine-tuning esperan volumen. Tarjeta **SEGUIMIENTO** = checklist de validaciones pendientes.

**Sesión 2026-07-14/15 (docs agregadas 2026-07-26 en reconciliación; tarjetas Kanban 1: 143/144/145/147)**: · **FIX-DASHBOARD-DATE-FILTER (143, ✅ Hecho; Validated pendiente SOLO de visto visual — ítem en SEGUIMIENTO K2-9)** bug de TZ en `useClientJobs`: `new Date(fechaHasta)` parsea UTC pero `setHours` es local (UTC-3) → filtrar el dashboard por UNA sola fecha excluía ese día, y el rango vacío mostraba el error crudo `{"` (PGRST103). Fix: cota superior `created_at < (fechaHasta + 1 día)` parseando `yyyy-MM-dd` a mano (mismo criterio que `get_dashboard_metrics`) + PGRST103 manejado como página vacía. El contrato `yyyy-MM-dd` se preservó al migrar a DatePicker shadcn (6f3eae9). · **INT-DUPLICADOS-FOLDER (144, ✅ Hecho + Validated 2026-07-26 con caso real)** carpeta `duplicados/` en todas las integraciones: se crea al conectar (`SYSTEM_INIT_FOLDERS` + onboarding Drive + test-connection) y `integration-file-mover.mjs` rutea el archivo del doc duplicado a `duplicados/` en vez de `procesados/` (`targetFolder = isDuplicate ? 'duplicados' : (success ? 'procesados' : 'fallidos')`; `worker.mjs` consulta `is_duplicate` de la fila; `file_location.processed_path` se persiste solo tras move exitoso). Deploy 4f9e69d. Validado en prod con caso real de Menara (fila 4178, 2026-07-23): `processed_path = a_procesar/duplicados/30-71081650-2_0122-00026411_01.pdf`. Cargas manuales/api_direct no mueven archivo (correcto: no hay integración de entrada). · **FIX-DASHBOARD-WARNINGS-COUNT (145, ✅ Hecho + Validated 2026-07-26)** el tablero mostraba SIEMPRE 0 advertencias: `finalizeJob` no escribía `pdf_jobs.low_confidence_documents` (la columna que `get_dashboard_metrics` suma como `documents_with_warnings`). Fix: `post-processor.mjs:finalizeJob` escribe `low_confidence_documents = warnRows` (deploy e5bd902) + backfill de jobs existentes. Verificado en DB 2026-07-26: jobs con contador >0 = filas `doc_status='warning'` (match exacto). · **PROMPT-IMPORTES-INTERPRETIVE (147, ✅ Hecho + Validated 2026-07-26 por comportamiento)** SYSTEM_PROMPT adaptado ADITIVO (sobre la apertura "analista que interpreta" de AFIP-CODE-CONSISTENCY): regla general de interpretación de importes (no copiar por etiqueta; analizar la relación subtotal/neto/IVA/exentos/percepciones/total) + `neto_gravado` por pasos (nunca usar el TOTAL como neto si hay exentos/percepciones) + `iva` (null en B/C sin discriminar, no estimar). Deploy 6a20414. La observación pendiente ("próximas facturas B/C variadas") ya ocurrió: las tandas posteriores (PRESSER 10/10, Loma Negra, GRAFER, Litoral Vial/BEVILACQUA) cerraron con importes correctos.

**Sesión 2026-06-23**: **TASK-92** estados de documento estandarizados (trigger `classify_pdf_job_row` reescrito: `failed` solo si error real o leído sin ningún dato; baja confianza/campos faltantes → `warning`; respeta `approved_at`; fix bug `NULL IN(...)`. Vocabulario unificado a **Exitoso/Con advertencia/Fallido**. Columna "Estado" por-documento en `DocumentsTable`). · **TASK-93** aviso de discrepancia (`getDocDiscrepancy` + `JobDiscrepancyNotice` en ProcesoDetailPage; gap real vs anomalía de conteo; no lista nombres → TASK-109) + **UX-JOB-ALLFAILED** (job 100% fallido → "Fallido" en JobList/JobDetailHeader/JobStatusBadge/MonitoringPage). · **TASK-94 + TASK-97** período = mes de procesamiento universal (trigger `trg_set_pdf_job_period`, backfill hecho) + quitado el selector Mes/Año del subidor. · **TASK-95** Activity vacía: causa raíz = RPC `get_tenant_jobs_admin` con `id` ambiguo (migración `fix_get_tenant_jobs_admin_ambiguous_id`) + empty-state + manejo de error. · **TASK-96** carpeta de errores en Drive (no-soportados → `fallidos/`; + soporte `.rar`). · **TASK-110** archivos rechazados visibles como proceso fallido (universal, todas las integraciones): RPC `gateway_register_rejected_file` + `error_type='rejected'` + helper compartido `registerRejectedFile`; el front ya los muestra como Fallido con la razón (nombre + formato). · Builds: `main-8UVxgJRP.js`, `main-D8Ivc5LK.js`; worker f4987cb+.

**Previas**: TASK-73 (Excel acumulativo Drive) · TASK-78 (Drive por cliente) · TASK-79 (`input_source` + filtro cliente) · TASK-80 (edición/aprobación manual de docs con error) · TASK-81 (saldo USD) · TASK-82 (panel Monitoreo superadmin) · TASK-83 (resiliencia frontend ante extensiones, 6 fixes → reglas en ZONA CERRADA/Auth) · TASK-84 (MonitoringPage Tenants vía RPC bypass RLS) · TASK-85 (MP webhook IPN, validado e2e) · TASK-87 (RLS `integration_processed_files`) · TASK-90 → **UX-PRICE-BREAKDOWN** (precio base configurable; `get_price_breakdown()`) · TASK-91 (validación email/CUIT duplicado) · REG-TAXID (CUIT obligatorio en registro) · TASK-104 (`polling_interval_tiers`) · TASK-106 (Supabase Storage e2e) · TASK-107 (init-folders) · INT-TEST-CONNECTION · INT-FOLDER-MIGRATION · FIX-GATEWAY-METADATA (spread de `metadata`) · Sentry · UX-BALANCE · FIX-AUTH-LOAD / FIX-AUTH-LOCK (reglas en ZONA CERRADA/Auth) · FIX-REG (trigger crea org+profile).

**Sesión 2026-06-24**: **TASK-98** tipo de documento como dropdown desde tabla `document_types` (no hardcode; muestra label, guarda code; build main-DCAXe0jM.js). · **TASK-99** aclaración ZIP/Drive en el cargador manual (`SubirZipPage`). · **TASK-100** drag & drop de 1 archivo en el cargador (validación + feedback visual). · **TASK-101** un solo punto de entrada 'Nuevo proceso' (quitado ítem 'Subir archivos' del nav; renombrado el botón de AppHomePage). Build main-DF8oXcT4.js. · **TASK-112** TODOS los dropdowns unificados a shadcn Select (0 `<select>` nativos en la app): cliente en Nuevo proceso, filtros cliente Dashboard/MisProcesos/Documentos (centinela `__all__`), tipo de doc en EditRowModal, y los 5 de IntegracionesPage (centinela `__root__` para raíz, polling number→string; solo estético, lógica de config intacta). · **TASK-114** visor read-only del prompt del worker en Monitoreo (`export SYSTEM_PROMPT` + gateway `GET /api/prompt` + tarjeta/modal superadmin; editar = futuro vía DB, DEC-018/TASK-113). · **TASK-111 (✅ Hecho + Validated en prod)** panel admin de `document_types` en MonitoringPage→Tipos de doc (tarjeta + modal: label editable, toggle activo, alta code+label). DB: RPCs `upsert_document_type`/`toggle_document_type` (SEC DEFINER + guard superadmin, `code` inmutable, sort_order default max+10). Validado con sesión superadmin: alta de tipo 'NIF' → escrito en DB (sort_order 130); toggle → active=false. `MonitoringPage.tsx` se truncó al editar (5º caso OneDrive) → reconstruido desde git HEAD + reaplicación en python, verificado (esbuild+tsc+balance+diff aditivo 150+/1-). · **FIX-DOCS-PAGINATION (✅ Hecho + Validated en prod)** bug preexistente en `DocumentsPage`/`useAllDocuments.ts`: `totalCount` sumaba facturas + TODAS las OCs (sin paginar) pero `.range()` solo paginaba facturas → página fantasma que pedía facturas fuera de rango → PostgREST 416 mostrado como error crudo `{"…`. Fix: las OCs se traen scoped a las facturas de la página (`.in('row_id', facturaRowIds)`) y `totalCount = count(facturas)`. Elimina el 416 y la duplicación de OCs entre páginas. Solo frontend; esbuild+tsc+balance OK. `useAllDocuments.ts` también se truncó al editar (6º caso OneDrive, archivo chico) → reconstruido desde git HEAD + python. · **REMOVE-DEDUP / DEC-019 (✅ Hecho + Validated en prod)** a pedido del director: quitada la deduplicación de los 3 pollers de integración (Supabase/Firebase/Drive). Antes, un archivo cuyo SHA256 (o drive_file_id) ya estaba en `integration_processed_files` se borraba/saltaba sin procesar; ahora se levanta y procesa todo. Seguridad anti-recobro: encolar SOLO si el move root→`en_proceso/` tuvo éxito (Supabase: `moveFile` retorna bool + `if(!moved) continue`; Drive: flag `moved` + gate; Firebase ya gateaba). Nombres tal cual (sin prefijo): repetidos pueden trabarse en `en_proceso/` al moverse a `procesados/` (asumido). `integration_processed_files` y sus RPCs quedan (no se borran). node --check + balance + git diff OK (sin truncado). · **TASK-108 (✅ Hecho + Validated en prod)** SCOPE-ATTACHMENT-EXTRACTION: la extracción de adjuntos embebidos se gatea por org vía `tenant_feature_flags.extract_embedded_attachments` (default false; Aignition=true). DB: columna aditiva + Aignition ON (migración `add_extract_embedded_attachments_flag`). Worker: helper `getExtractAttachmentsFlag` en `worker.mjs` + param/gateo en `zip-processor.mjs` (OFF → no extrae → 0 OCs, no cobra). node --check + balance OK. `worker.mjs` y `zip-processor.mjs` se truncaron al editar (7º/8º casos OneDrive) → reconstruidos desde git HEAD + python. **Toggle UI**: columna 'Adjuntos' (switch por tenant) en MonitoringPage→Balance por tenant, vía RPCs `get_tenant_attachment_flags`/`set_tenant_attachment_extraction` (SEC DEFINER + guard superadmin, upsert). Prender/apagar la extracción por cliente sin SQL. · **TASK-109 (✅ Hecho + Validated en prod)** JOB-FILE-MANIFEST (fase 2 de TASK-93): el worker registra el manifiesto de archivos detectados por job en `pdf_jobs.file_manifest` (jsonb nullable, columna aditiva). `zip-processor` devuelve `detectedFiles` (=`allFiles`); `worker.mjs` arma `[{name,status}]` (processed/failed/upload_failed/omitted) y lo pasa a `finalizeJob` (lo escribe en el PATCH). Frontend: `JobDiscrepancyNotice` lista por nombre los no-`processed` cuando hay manifiesto; jobs viejos sin manifiesto = solo conteo (backward-compat). esbuild+tsc+balance OK; .mjs editados vía python (sin truncado). **Extensión (mismo no-deploy):** (1) **RAR arreglado** — p7zip de Alpine NO trae códec RAR (extraía vacío → job 'exitoso' sin nada); Dockerfile usa `7zip` (`7z`/`7zz` para ZIP/7z; se quitó `p7zip` que conflictúa en `/usr/bin/7z` y rompía el build) **+ `libarchive-tools`** (`bsdtar`, lee RAR5, OSS): la build OSS de 7zip en Alpine NO trae el códec RAR (propietario). `zip-processor` extrae con `7zz` y, si no sale nada (caso RAR), cae a `bsdtar`; loguea `zip.extract_result` por herramienta. (`unrar`/`unar` NO están en los repos de Alpine; `unrar-free` no soporta RAR5.) (2) **'exitoso vacío' eliminado** — si una subida ZIP/RAR extrae 0 archivos (ni soportados ni no), worker tira `TerminalError` → job Fallido con razón. (3) **No soportados nombrados** — `zip-processor` colecta los archivos no soportados dentro del comprimido (find busybox-safe, excluye `adj/`) y los suma al manifiesto con status `unsupported`; `JobDiscrepancyNotice` nombra TODOS los no-`processed` (incl. no soportados) aun sin hueco de conteo. ⚠️ Requiere rebuild del worker (Dockerfile cambió). · **TASK-105 (✅ Hecho + Validated en prod)** UX-OUTPUT-FORMAT: el toggle de Salida automática + selector CSV/Excel/JSON en `IntegracionesPage` **ya existían** (output_enabled/output_format al RPC, + disclosure de xlsx); lo que faltaba era el **precio dinámico**: se carga `feature_pricing_multipliers` (`master_file`) y se muestra `(+$X/doc)` junto a la opción Excel **solo en google_drive** (donde aplica master_file). Solo frontend; esbuild+tsc+balance OK. NO se colapsó a un solo toggle (el diseño actual permite también salida CSV). **Coherencia de nombres**: la opción de formato Excel se renombró a **"Excel acumulativo (.xlsx)"** (solo el texto visible; `value="xlsx"` intacto) para que coincida con la línea **"Excel acumulativo"** del desglose en Configuración→Costo por documento (label del feature `master_file`). Adaptación de lo nuevo a lo existente, sin tocar la lógica de la integración. · **FIX-OUTPUT-COHERENCE (mismo no-deploy):** (a) **DB** — `get_price_breakdown` ahora suma `master_file`/`xlsx_output` SOLO si `output_enabled=true` (antes solo miraba `output_format='xlsx'`, así que con la salida desactivada pero formato xlsx viejo seguía **mostrando** el cargo; era bug de display, el cobro real ya gateaba por output_enabled vía `admin_get_output_integration`). Migración `fix_get_price_breakdown_require_output_enabled`, en vivo. (b) **Frontend** — al desactivar el toggle 'Salida automática', el formato vuelve solo a `csv` (claridad + evita dejar xlsx colgado). esbuild+tsc+balance OK. · **TASK-86 (✅ commit 79eda82, FIX-CLEANUP cerrada 2026-06-28: + worker.mjs:34 fallback GATEWAY_URL → api.agoradigital.io; 0 refs automation.aignition en el código)** FIX-CLEANUP: limpiadas las refs n8n obsoletas del código (2 comentarios que decían 'sub-workflow' → ahora 'document-processor directo', en `worker.mjs` y `zip-processor.mjs`) + se borran del repo 3 exports muertos de n8n en la raíz (`temp_workflow.json`, `Aignition PDF → Excel Data Laundering v2.json`, `workflow_subworkflow_v4.json`) vía `git rm`. NO se tocan las URLs `automation.aignition.net/worker` (dominio del gateway del worker, funcional) ni los comentarios `DEC-011: N8N eliminado` (históricos correctos). Pendiente opcional: limpiar el tag 'n8n' del Kanban. · **TEST-CLASSIFY-TRIGGER (✅ Hecho + Validated)** harness de tests del trigger `classify_pdf_job_row`: función `test_classify_pdf_job_row()` (+ helper `test_clt_eval`) en DB (migración `add_test_classify_pdf_job_row`) + archivo versionado `sql/tests/test_classify_pdf_job_row.sql`. **16/16 PASS** contra el trigger actual; cubre todas las ramas + casos borde (conf NULL→ok, límite 0.70→ok, `last_error_message` NULL safe = bug histórico TASK-92). Seguro en prod (subtransacciones que se revierten; 0 huérfanos). Solo DB; no requiere deploy de worker/frontend. · **WARNING-REASON (✅ backend en prod 2026-07-10, frontend pendiente deploy)** el trigger `classify_pdf_job_row` ahora setea también `pdf_job_rows.warning_reason` (columna aditiva; migración `add_warning_reason_to_classify`) con la causa del warning: `IMPORTE_NO_CIERRA` (chequeo aritmético), `CAMPOS_FALTANTES` (<4 campos clave), `BAJA_CONFIANZA` (conf<0.70), `DATOS_INCOMPLETOS` (last_error_message), `REVISION` (fallback); NULL si ok/failed. **La lógica de `doc_status` quedó IDÉNTICA** (mismo `v_low_quality`, solo se separó en `v_extractor_flag`/`v_conf_low`/`v_arith_fail` para nombrar la razón). Harness `test_classify_pdf_job_row()` **16/16 PASS** (doc_status intacto). Backfill de warnings históricos hecho (UPDATE no-op scoped). Frontend: helper `warningReasonLabel` en `documentClassification.ts` + razón legible al lado del badge en `DocumentsTable` y `JobDocumentsSection` (selects ya traían `*`). esbuild+tsc(solo estos archivos, 0 errores nuevos)+balance OK; front vía python. ⚠️ Requiere build+scp del frontend. Sigue **DESCUENTO-CAPTURE** (capturar el descuento para que las facturas con bonificación cierren; hoy caen en IMPORTE_NO_CIERRA). · **DESCUENTO-CAPTURE (código hecho 2026-07-10, pendiente deploy+validación)** columna aditiva `pdf_job_rows.descuento` (migración `add_descuento_to_pdf_job_rows`). Prompt (`document-processor.mjs`) **adaptado ADITIVO** (no reescrito; puntos 1-5 de neto_gravado intactos): punto 6 = neto_gravado es la BASE POST-DESCUENTO (modelado AFIP → neto+IVA=total reconcilia solo) + campo `descuento` + clave `"descuento":null` en ESTRUCTURA EXACTA. `insertJobRow` mapea descuento. Salida `output-depositor.mjs`: `descuento` en COLUMNS + label 'Descuento' (CSV+xlsx, acumulativo re-mapea). **DECISIÓN (director): descuento → ADVERTENCIA SOLO SI la cuenta no cierra.** Una factura con `descuento` que reconcilia (dentro de la tolerancia 1% del chequeo aritmético) sigue **`ok`/exitosa** con el descuento poblado; solo si el chequeo aritmético FALLA **y** hay descuento → `warning` con razón **`REVISAR_DESCUENTO`** (más accionable que `IMPORTE_NO_CIERRA`, el descuento suele ser la causa). Sin descuento + arith-fail → `IMPORTE_NO_CIERRA` como antes. `approved_at` sigue ganando → ok. Trigger `classify_pdf_job_row`: migración `descuento_forces_warning_in_classify` (1er intento: descuento forzaba warning siempre) **corregida por `descuento_warning_only_if_arith_fails`** — `v_has_descuento` solo cambia la RAZÓN cuando `v_arith_fail`, NO fuerza warning por sí solo. Harness **18/18** (`test_clt_eval` +param opcional `p_desc`; casos 17 descuento+cuenta cierra→ok, 18 descuento+aprobado→ok; `harness_fix_descuento_cases`). **Global** (no gateado por tenant). Front (ya deployado): label `REVISAR_DESCUENTO`='Tiene descuento — revisá los importes' en `warningReasonLabel`. · **NETO-FROM-IDENTITY (2026-07-23):** cuando la factura imprime **Subtotal + Bonificación SIN línea de 'Neto Gravado'** (caso BEVILACQUA/Litoral Vial), el modelo CALCULA neto=subtotal−descuento y a veces yerra la aritmética (dio `1039977.26` en vez de `1040077.26`, error de 100; los LLM restan mal). Fix determinístico en `processDocument`: **solo si hay descuento** y están total+IVA, se **deriva `neto_gravado = total − iva − exento − percepciones − imp.internos`** (identidad AFIP) → cierra exacto. **Salvaguarda**: solo corrige si el desvío vs el neto del modelo es >0.5 y < max(1000, 2% del total) (ajuste de aritmética, no estructural — así no rompe facturas con conceptos no contemplados, ej. Intereses). Sin descuento el neto impreso NO se toca (cero regresión). Efecto: los descuentos que antes caían en `REVISAR_DESCUENTO` por el corrimiento del neto ahora cierran y salen `ok` con el neto correcto. 6/6 casos (Litoral Vial + PRESSER 4733/4725 corregidos; sin-descuento / ya-cierra / desvío-grande intactos). · **PV-4-DIGITOS (2026-07-23):** `normalizePuntoVenta` recorta un PV de **5 dígitos que arranca en 0** (`00004→0004`, formato de vendedores tipo Litoral Vial) y mantiene los PV genuinos de 5 (`10039`). Motivo: COMPROBANTE-FROM-OCR dejaba `00004` (fiel al impreso `A00004-00012276`); el director pidió canonizar a 4 dígitos. · **PERC-IIBB-IDENTITY + EDIT-FIELDS + PROMPT-HINTS (2026-07-24, caso Loma Negra/GRAFER de Menara):** (1) Loma Negra imprime las percepciones IIBB en **varias líneas por jurisdicción** (BS.AS.+NEUQUEN+SALTA) y el modelo erraba la SUMA (+100/+200) → bloque nuevo en `processDocument`: **sin descuento**, con `percepcion_ingresos_brutos`≠0 y desvío CHICO (>0.5, <max(1000,2%total)), se deriva percIIBB por identidad (`total−neto−iva−exento−percIVA−internos`); mutuamente excluyente con NETO-FROM-IDENTITY (ese corre solo CON descuento). 7/7 casos. (2) `EditRowModal` ganó los campos **Perc. IVA / Perc. IIBB / Imp. Internos** (faltaban; los GRAFER con IDC/ITC no se podían corregir para aprobar). (3) Prompt ADITIVO: percIIBB puede venir multi-línea por jurisdicción → SUMAR todas; en COMBUSTIBLES los renglones **IDC/ITC son impuestos internos** (GRAFER: IDC 1635.11 + ITC 14346.36; el modelo no reconocía las siglas → null → IMPORTE_NO_CIERRA). · **AI-CORRECTIONS-DATASET (2026-07-25, solo DB — épico AI-IMPROVEMENT, Kanban Parte 2):** tabla **`extraction_corrections`** (RLS select_own_org; row_id FK cascade; original/corrected jsonb + changed_fields) + trigger **`trg_capture_extraction_correction`** (AFTER UPDATE en `pdf_job_rows`, SEC DEFINER, exception-swallow) que captura el par (extracción IA → corrección humana) cuando un usuario edita un doc. ⚠️ El discriminador de 'edición humana' es **`auth.uid()` presente** (worker/service key = null → no captura); NO se puede anclar en `doc_status='pending_approval'` porque `classify_pdf_job_row` (BEFORE) lo recalcula y lo pisa. Solo anota los ~23 campos editables que cambiaron; el OCR se joinea vía row_id. Tests 5/5. Base del flywheel: perfiles por proveedor / few-shot / fine-tuning consumen esta tabla. · **AI-PROVEEDOR-PROFILE (2026-07-25, ✅ validado en prod):** tabla global **`proveedor_profiles`** (cuit 11 dígitos único, `prompt_hints` en español, active; RLS read authenticated; gestión vía RPC **`upsert_proveedor_profile`** superadmin — alta/edición SIN deploy, efecto inmediato con cache 10 min). El worker (`document-processor.mjs`) cachea los perfiles activos (`getProveedorProfiles`, TTL 10 min), matchea el CUIT contra el OCR normalizado a dígitos (`matchProveedorProfiles`, exportada) y si hay match agrega los hints al user message en bloque delimitado `[PERFIL DEL PROVEEDOR …]` (log `prov_profile.matched`). **Sin match = byte-idéntico a antes**; fail-safe total. **Seed**: 5 proveedores con mañas conocidas (Loma Negra percepciones multi-jurisdicción, GRAFER IDC/ITC+eslogan, Elsener comprobante pegado, Bevilacqua/Litoral Vial subtotal−bonificación, Navone Ing.Brutos-no-es-PV). Validado: Loma Negra 138256 (perc 78238.24, gap 0) y GRAFER 49-0086 (internos 15981.47, gap 0), ambos con match logueado. **Loop de mejora**: revisar `extraction_corrections` periódicamente → patrón recurrente → `upsert_proveedor_profile`. · **WORKER-RESILIENCE (2026-07-25, código hecho — pendiente deploy+validación):** 4 casos reales de jobs 'trabados' en processing (El CEDRO fetch terminated, ELECT.RAF., ACERMA Mistral 503, 33709838429 URL de storage no propagada 400): error transitorio + `attempts:0` (sin retry) + `on('failed')` que solo logueaba → fila huérfana. Fix doble: (1) `gateway.mjs` encola con **`attempts:3` + backoff exponencial 5s** (los TerminalError —sin saldo, ZIP vacío— NO reintentan: van como UnrecoverableError); (2) `worker.mjs on('failed')` ahora **persiste la falla FINAL** vía `failJob` (PATCH idempotente) cuando no es terminal (el terminal ya la marca el catch) → nunca más filas huérfanas. Los 4 casos históricos se destrabaron a mano (UPDATE manual). ✅ Validado en prod: 10 PRESSER reales → todas `ok` salvo una extracción con gap 700>tol 584 → `REVISAR_DESCUENTO` (correcto). Pendiente: commit worker (`document-processor.mjs`+`output-depositor.mjs`). · **CSV-OUTPUT-FORMAT (TASK-122, ✅ Hecho + Validated en prod)** el CSV depositado (`output-depositor.mjs`) ahora: separador `;` (antes `,`; escapeCSV cita por `;`), sin la columna `iva` genérica (vía `CSV_COLUMNS`), y encabezados legibles de alícuotas (`iva_21`→'IVA 21%', `iva_105`→'IVA 10,5%', `iva_27`→'IVA 27%', `iva_5`→'IVA 5%', `iva_25`→'IVA 2,5%'). También el **xlsx** (per-job + acumulativo en Drive) usa el mismo criterio (OUT_COLUMNS/OUT_LABELS): el acumulativo **re-mapea las filas existentes** a los encabezados nuevos al regenerar el archivo (dropea `iva`, renombra alícuotas; **migra archivos viejos** en el próximo append, idempotente), preservando el histórico. `COLUMNS` y el SELECT quedan intactos; todo el cambio vive en la capa de salida. El frontend `csvExport.ts` ya usaba `;` y tiene una sola columna 'IVA' → sin cambios. node --check+balance OK + simulación del re-mapeo verificada. · **FILE-RENAME-BY-DATA (TASK-123, Fase 1, ✅ Hecho + Validated en prod 2026-06-26)** los archivos de **storage (Supabase/Firebase)** se nombran por dato cuando el job es de **1 documento** con los 3 datos clave → `{cuit}_{numero_comprobante}_{codigo_afip}.{ext}` (saneado). Aplica a (a) el **PDF de entrada** al moverse a `procesados/` (`integration-file-mover.mjs`, gateado por `renameBase` que arma `worker.mjs:getSingleDocRenameBase` consultando la fila) y (b) el **CSV de salida** (`output-depositor.mjs`). Criterio único en helper compartido nuevo **`worker/doc-naming.mjs`** (`buildDocFileBase`/`sanitizeNamePart`; null si falta algún dato → no renombra). **Drive queda como está** (acumulativo `resultados.xlsx` + move por ID; decisión del director). **Multi-doc (ZIP)** y **datos faltantes** = sin cambios (Fase 2: `en_revisión/` + edición). **Duplicado:** la combinación cuit+numero es única; si el destino ya existe (mismo nombre) → NO se pisa el original, el archivo se mueve con marca visible `__DUPLICADO_{ts}` + alerta `file_mover.duplicate` en log (Supabase detecta colisión por respuesta del move; Firebase con `.exists()`). Pendiente follow-up chico: badge 'duplicado' en el front. CSV de salida en Supabase sigue con `x-upsert:true` (reproceso de la misma factura regenera el mismo CSV). 4 archivos, node --check + balance + simulación de naming/regex OK; editados vía python (sin truncado). Deploy: rebuild worker (archivo nuevo `doc-naming.mjs`). · **FILE-RENAME-BY-DATA Fase 2 (TASK-123, Opción A, ✅ Hecho + Validated en prod 2026-06-26)** completa el flujo de duplicados y del documento incompleto, **storage only** (Supabase/Firebase; Drive sin cambios). **DB (3 columnas aditivas):** `pdf_jobs.file_location` jsonb (ubicación del input tras moverse a procesados/: `{integration_id,protocol,bucket_name,processed_path}`), `pdf_job_rows.is_duplicate` bool, `pdf_jobs.has_duplicate` bool (flag a nivel proceso para el ⚠️ del dashboard) + backfill. Migraciones `add_file_location_and_is_duplicate`, `add_pdf_jobs_has_duplicate`. **Duplicado = misma factura (cuit+numero) ya procesada antes en la org** (fila con `id` menor; chequeo `hasEarlierDuplicate` en `output-depositor`). Comportamiento del duplicado: **se procesa y se cobra** (usó el servicio), el PDF queda en `procesados/` con `__DUPLICADO_{ts}`, se marca `is_duplicate`(fila)+`has_duplicate`(job), y **NO se deposita CSV de salida** (skip en `output-depositor`, tanto en el depósito automático como en `depositSingleApprovedRow`) para que el sistema del cliente no levante un registro repetido. **Opción A — rename in-place al aprobar:** un documento incompleto (le falta alguno de los 3 datos) NO se renombra; el usuario completa+aprueba desde el front (`/api/deposit-row` ya existía) y ahí `renameProcessedInputOnApproval` (en `integration-file-mover`) renombra el input en `procesados/` con los datos definitivos. NO se cambió el ruteo del worker (el archivo sigue yendo a procesados/ como siempre). **Gap preexistente arreglado:** `depositSingleApprovedRow` no tenía rama `supabase_storage` (aprobar en Supabase no depositaba salida) → agregada. **Frontend:** badge 'Duplicado' (texto) en `DocumentsTable`, `JobRowsTable`, `JobDocumentsSection` (este último además con **recuadro naranja `DUP_ROW_STYLE` + mensaje** 'no se generó CSV por duplicado'); **ícono `AlertTriangle` (lucide SVG, no emoji)** al lado del badge de Estado en el dashboard (`ClientDashboardPage`, tabla inline — NO usa `JobList`) y en Mis Procesos (`JobList`). `has_duplicate` agregado al tipo `PdfJob` + selects de `usePdfJobs`/`useClientJobs`. Archivos worker: `output-depositor.mjs`, `integration-file-mover.mjs`, `worker.mjs`, `gateway.mjs` (+`doc-naming.mjs` de Fase 1). node --check + balance + esbuild OK; .mjs/.tsx vía python (sin truncado). Deploy: rebuild worker + frontend. · **BUG-FIX `approve_document_row` (charge_credit ambiguo, ✅ en prod 2026-06-26)** latente preexistente (NO causado por las columnas nuevas): `approve_document_row` llamaba `charge_credit` con **5 args nombrados**, pero hay 2 overloads (5 y 6 args, ambos con defaults; el de 6 lo sumó TASK-104) → resolución **ambigua** `42725 function charge_credit(...) is not unique` → la RPC daba **400** y bloqueaba la aprobación manual. Saltó ahora porque fue la 1ª vez que se ejercitó editar+aprobar desde que existe el overload de 6. **Fix** (migración `fix_approve_document_row_charge_credit_ambiguous`): pasar el 6º arg explícito `p_polling_interval_minutes => NULL` → resuelve unívocamente al overload de 6 (superset exacto del de 5 con polling NULL → costo idéntico; el cobro no cambia). Solo DB, en vivo, sin deploy. El worker (`post-processor.mjs`) ya llamaba con 6 args, no afectado. · **PERF-FRONT-LOAD (✅ Hecho + Validated en prod 2026-06-27)** optimización de carga inicial del front. (1) **Lazy-load de rutas**: las 13 páginas de `App.tsx` pasan a `React.lazy` + `<Suspense>` (patrón `.then(m=>({default:m.X}))` por named exports) → cada página es su chunk. (2) **`xlsx` dinámico** en `utils/excelExport.ts` (`await import('xlsx')`, función async) → sale del bundle inicial, se baja solo al exportar. Resultado: bundle inicial **1.098 KB → 529 KB** (~159 KB gzip); xlsx (429 KB) + Monitoring/Integraciones = chunks por demanda. (3) **Servidor (Caddy)**: el front lo sirve **Caddy en Docker `n8n-caddy-1`**, Caddyfile en el host en **`/opt/n8n/caddy/Caddyfile`** (NO el caddy del sistema, que está inactivo). gzip ya activo; se agregó `Cache-Control: public, max-age=31536000, immutable` para `/assets/*` (hash en nombre = cache-bust) y `no-cache` para `index.html` (revalida siempre → toma deploys nuevos). ⚠️ regla operativa: nunca cachear `index.html` largo o los usuarios no ven deploys. Opcional pendiente: `Sugar-Pie.ttf`→`.woff2`. Sigue **PERF-DASHBOARD-SCALE** (paginar dashboard + métricas por agregado en DB; `useClientJobs` hoy trae todos los procesos sin paginar) — registrada en Kanban. · **PERF-DASHBOARD-SCALE (✅ Hecho + Validated en prod 2026-06-27)** el dashboard ya no trae todos los procesos del tenant. **Paso 1 — métricas por agregado en DB**: RPC `get_dashboard_metrics(p_client_id, p_fecha_desde, p_fecha_hasta)` **SECURITY INVOKER** (se apoya en el RLS de `pdf_jobs`, sin bypass; migración `add_get_dashboard_metrics`) devuelve los 9 agregados (jobs_count, total/processed/failed/warning/corrected docs, jobs_with_error, duplicate_documents, total_execution_ms). `useClientJobs` pide las métricas al RPC (inicial + Realtime/polling debounced 350ms); `calculateMetrics` quedó sin uso. **Paso 2 — tabla paginada**: `useClientJobs` reescrito con `loadPage()` (`range` + `count:exact`), **10 por página**; Realtime/polling = **refetch debounced de la página visible** (ya no mantiene todo el array en memoria). `ClientDashboardPage`: controles Anterior/Siguiente (aparecen al pasar de 10). Las tarjetas de métricas cubren todo el tenant (RPC), la tabla solo la página. Filtros resetean a página 1. Preservados: guards de auth/loading, canal `pdf_jobs_changes`, polling de respaldo. Solo frontend + 1 migración (RPC); esbuild+balance OK. · **DASHBOARD-PERFORMANCE-CARD (✅ en prod 2026-06-27/28)** la card 'Performance' del dashboard muestra: Tiempo total de ejecución (`finished_at-created_at`), Promedio por documento, **Efectividad de extracción (tenant)** y **Efectividad del sistema (global)**. La efectividad = promedio de `pdf_job_rows.confidence_score` (0..1, lo escribe el OCR/IA), mostrado como %. Tenant: `get_dashboard_metrics` ahora devuelve `avg_confidence` (subquery sobre las filas de los jobs filtrados; SECURITY INVOKER, RLS). Global: RPC nueva **`get_system_avg_confidence()` SECURITY DEFINER** (promedio sobre TODAS las orgs; devuelve solo el número agregado, no expone datos de otros tenants) — se trae una vez, comparar tenant vs sistema ayuda a detectar si un cliente está por debajo de la media (problema de calidad del cliente vs problema general). Migraciones `dashboard_metrics_add_avg_confidence` (DROP+CREATE) y `add_get_system_avg_confidence`. · **woff2 fuente Sugar Pie (✅)** `public/fonts/Sugar-Pie.woff2` (43 KB vs 127 KB ttf); `@font-face` usa woff2 + ttf fallback. · **calculateMetrics** quedó eliminado de `useClientJobs` (ya no se usa, las métricas van por RPC). · **DASHBOARD-COSTO-CARD + toggle integración (✅ en prod 2026-06-28)** la 3ª card del dashboard ('Costo por documento') muestra el desglose de precio del tenant vía `get_price_breakdown()` (mismo dato que Configuración; precios dinámicos, no hardcode): precio base + features activas + polling + total. Debajo, una fila por **integración activa** (solo las `is_active=true`, que es lo que ya devuelve el desglose) con un **switch** para activar/desactivar. El toggle llama RPC nueva **`set_integration_active(p_integration_id, p_active)`** (SECURITY DEFINER + guard: solo integraciones de la org del `auth.uid()`); tras el toggle se recarga precio+integraciones, así el costo se actualiza al instante. **`is_active` ya estaba cableado** end-to-end: el poller usa `admin_get_active_integrations` (filtra is_active) y `get_price_breakdown` también → desactivar **frena el polling de esa integración y deja de cobrar su feature**, sin tocar el worker. Al desactivar, la fila desaparece (el desglose solo muestra activas); re-activar = desde IntegracionesPage. UI con `Switch` (componente existente, `checked`/`onChange`). Solo frontend (`ClientDashboardPage`) + 1 RPC. · **NUM-COMPROBANTE-NORMALIZE (✅ Validated en prod 2026-07-04)** `pdf_job_rows.numero_comprobante` ahora guarda SOLO el **correlativo** (lo de después del último `-`): la IA a veces armaba `punto_venta`+`-`+raw y el raw ya traía la sucursal+letra → salía duplicado (`0004-A00004-00012150`). Normalización **determinística** en `document-processor.mjs` (aplicada a `extracted.numero_comprobante` en `processDocument` tras la extracción, antes de insertar) → `00012150`. La sucursal queda en `punto_venta`. **Nombre de archivo y dedup siguen branch-aware**: `buildDocFileBase` (doc-naming) reconstruye `punto_venta-correlativo` y `hasEarlierDuplicate` (output-depositor) suma `&punto_venta=eq.` (dedup más estricto). SELECTs de `worker.mjs`/`integration-file-mover.mjs` ganaron `punto_venta`. App y CSV muestran el correlativo solos (leen la columna, sin cambios de front). 5 archivos worker, node --check + tests OK. Filas viejas conservan el valor con doble (data de testeo). · **COMPROBANTE-SPLIT-AFIP (2026-07-14, reemplaza `correlativoDe`)**: el comprobante puede venir **PEGADO sin separador** (caso ELSENER: `003900075132` = 12 díg.) o con la **letra de clase** adherida (`A0039-00075132`); `correlativoDe` (cortaba después del último `-`) devolvía el string entero y el punto de venta salía mal. Nueva **`splitComprobante(numero)`** determinística → `{puntoVenta, correlativo}` según AFIP (**PV numérico 4-5 díg. + correlativo 8 díg.**): toma los bloques de dígitos ignorando letras/separadores; si hay ≥2 bloques, el **primero** es el PV y el **último** el correlativo; si hay **un solo bloque de >8 díg.**, los **últimos 8** son el correlativo y el resto el PV; si es ≤8, es solo correlativo y el PV lo aporta el modelo. **La LETRA A/B/C/M/E es la CLASE, NO el punto de venta** (ya está en `tipo_documento`/`codigo_afip`) → se descarta de estos campos; `punto_venta` queda **numérico** (helper `normalizePuntoVenta`, padding a 4). El PV derivado del número impreso **le gana** al que adivina el modelo. Ambas funciones exportadas; **9/9** casos de test OK. Sin backfill (el director limpió la DB de prueba). · **COMPROBANTE-FROM-OCR (2026-07-23):** el comprobante ahora se extrae **directo del texto OCR** como fuente de verdad, no del modelo. Motivo (caso NAVONE `A000200040110`): el modelo transpuso el correlativo (`00400110` en vez de `00040110`) y tomó el PV de un número suelto del cuerpo (`Ing. Brutos: 0000` → PV `0000`); como `puntoVentaFromOcr` se anclaba en el correlativo del modelo (equivocado), no lo recuperaba. Helper nuevo **`comprobanteFromOcr(ocrText)`** busca dos formatos AFIP inequívocos: (1) clase A/B/C/M/E + 12-13 díg. pegados (`A000200040110`), (2) `\d{4,5}-\d{8}` con separador; no matchea CUIT (`30-8díg-1`) ni CAE (14 díg.). En `processDocument` el comprobante del OCR **MANDA** sobre el del modelo (correlativo y PV); si no hay patrón claro, cae al modelo (comportamiento previo). Prioridad PV: OCR-comprobante → `puntoVentaFromOcr` anclado → PV embebido del modelo → PV del modelo. **6/6** casos (incl. NAVONE + no-regresión de Elsener pegado/separador/clásica). Validado en prod: reproceso NAVONE → PV `0002`, número `00040110` (vs lo impreso). ⚠️ El número impreso puede diferir del nombre de archivo del cliente (`00400110`); manda lo impreso. · **LINE-ITEMS-EXTRACTION (épico, Fases 1-3 en prod 2026-07-04; Fase 4 pendiente)** diferenciador: extraer el **detalle de renglones** (producto/cantidad/precio) que AFIP no da. **Fase 1** (02175c4): tabla nueva `pdf_job_row_items` (multitenant + RLS `items_select_own_org`; org_id, row_id FK→pdf_job_rows, descripcion/cantidad/precio_unitario/importe/orden) + extracción en `document-processor.mjs` (campo `items` en el prompt + `insertItems` best-effort + helper `toNum`). Extracción **SIEMPRE** (dato propio), aunque el tenant no pague. **Fase 2** (1c6c2e5): feature paga `line_items` en `feature_pricing_multipliers` ($0.10, editable) + flag por tenant `tenant_feature_flags.line_items_enabled` + RPC `set_tenant_line_items` (superadmin) + `get_price_breakdown` suma la línea cuando el flag ON. `charge_credit` **NO se tocó** (es genérica: `post-processor.mjs` agrega `line_items` a `activeFeatures` cuando el flag ON → cobra +$0.10/doc). **Fase 3** (234ed77): hook `useLineItems` (lee flag + items, RLS) + sección "Detalle de renglones" en `DocumentDetailModal`, **gateada** por el flag. **Fase 4** (en prod 2026-07-04): archivo de **productos** aparte, gateado por el flag — Drive **acumulativo** `productos.xlsx` (espeja `resultados.xlsx`) + per-job `{base}_productos.{ext}` para storage/SFTP/FTP (helper `depositProducts` en `output-depositor.mjs`; cols CUIT/Nro Comprobante/Descripción/Cantidad/Precio Unitario/Importe). Renombrado a **"Detalle de productos"** (label del feature + sección en la app). **Ajuste dedup (DEDUP-UNIVERSAL-MARKING):** el marcado de duplicados ahora corre en cualquier flujo con salida (Drive/manual, no solo storage), y un **duplicado NO deposita salida** (ni facturas ni productos) en ningún flujo — igual se procesa y se cobra. Follow-up dedup pendiente: cobertura para tenants SIN integración de salida (mover marcado a post-processor). Validado: extracción multi-renglón (URBANA=3 items fieles al impreso), cobro +$0.10 confirmado en `credit_transactions`. Nota billing: el fee de una integración (ej. Drive $0.20) solo se cobra cuando el doc entra POR esa integración, no en cargas manuales — el desglose lo muestra igual (inconsistencia display-vs-cobro pre-existente).